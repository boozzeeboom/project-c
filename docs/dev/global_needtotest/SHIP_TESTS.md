# Global Need-To-Test — реестр ручных тестов (долгосрочный)

> Каталог: `docs/dev/global_needtotest/`. Назначение: все проверки, которые агенту
> нельзя выполнить самому (Play Mode, два клиента, мод-клиент) — их проводит пользователь.
> Правило: каждый кодовый тикет (`T-SHIP-FIX*` и далее) дописывает сюда свои кейсы.
> Формат кейса: `[ ]` + шаги + ожидание + приоритет. Закрытые: `[x]` + дата + кем.
> Приоритеты: 🔴 блокер (регресс = откат), 🟡 критический (эксплойт/десинхрон), 🟢 рутина.

## T-SHIP-FIX03 — серверный прайс (коммит `116457f2`, 2026-09-18)

- [ ] 🟡 Продажа по-честному: док → E → продать модуль → начислено `max(1, costCredits/2)` из SO-модуля.
- [ ] 🟡 Подмена sell: `sellCredits=int.MaxValue` → начислен серверный прайс + Warning в консоли сервера.
- [ ] 🟡 Подмена cost: `cost=0/-100` (repaint/hull) → списаны 500/300, баланс не растёт.
- [ ] 🟢 Неизвестный модуль в слоте → продажа даёт 0 + Warning, без NRE.
- [ ] 🟢 Регресс: install (бесплатно, как было), sell/repaint/hull по честным ценам — флоу без ошибок.
- [ ] 🟢 Инспектор: `_serverRepaintCost/_serverHullRepairCost` на `ShipModuleServer` = ценам NPC
      (`RepairManager._repaintCost/_hullRepairCost`); при расхождении — выровнять (сервер главный).

## T-SHIP-FIX04 — Recall (коммит 2026-09-18, см. `docs/Ships/fix/T-SHIP-FIX04_recall.md`)

- [ ] 🟢 Свой recall: док → E → «Вызвать» (пад свободен рядом) → корабль на паде, кредиты − 500, freeze снят, взлетает.
- [ ] 🟡 Чужой recall (RPC на чужой `sc`) → отказ, без списаний и телепорта + Warning у сервера.
- [ ] 🟡 Подмена `cost=0` → списаны 500 (серверная цена).
- [ ] 🟡 Точка в чистом поле (пада рядом нет) → отказ, без списаний.
- [ ] 🟡 Все пады заняты → отказ, без списаний.
- [ ] 🟢 Кредиты после recall = до − 500 (сверить цифру).
- [ ] 🟢 Recall пристыкованного → `ExitDocked` + grace (столкновение в первые 3 сек не бьёт корпус).
- [ ] 🟢 Рестарт сервера <5 сек после recall → зафиксировать факт позиции (известный риск автосейва 5 сек).

## Проверки вердикта ревью (долгосрочные, после фаз B+C)

- [ ] 🟢 F8/rehost: модули и ящики на месте, подписки живы.
- [ ] 🟢 Heavy + расширители трюма: `cargoUsed/cargoMax` в HUD = серверному.
- [ ] 🟢 Перегрев мезий-модуля после переустановки в доке (без рестарта).
- [ ] 🟢 Install/sell с подменённым `sellCredits` (дубль FIX03, регресс).
- [ ] 🔴 Compile: Console → 0 errors после каждого тикета.

## T-SHIP-FIX01 — ввод через RPC (коммит 2026-09-18, см. `docs/Ships/fix/T-SHIP-FIX01_server-input.md`)

- [ ] 🟢 Пилот жмёт C/V (мезия-питч), Z/X (мезия-ролл + крен), Shift+A/D (мезия-ява), Shift+W/S (мезия-тяга), L (дозаправка стоя) — всё работает как раньше.
- [ ] 🟡 Второй клиент жмёт те же клавиши — чужой корабль стоит (было: двигался с хоста).
- [ ] 🟡 Хост жмёт C/V/Z/X/L — чужие корабли стоят.
- [ ] 🟢 IDLE-корабль без пилотов на L не заправляется.
- [ ] 🟢 NPC-курсирование без регрессий (маршруты, доки, обход).
- [ ] 🟢 Кооп: два пилота — ввод усредняется, без рывков.
- [ ] 🟢 Мод-клиент шлёт `thrust=1e6` → скорость как при 1.0 (clamp).
