# Floating origin — архитектура и поэтапное внедрение

Дата: 2026-09-09. Задача: полный переход Project C на локальные вычислительные координаты для MMO Host + Clients, с дальнейшей возможностью dedicated server.
Тикеты серии: **T-FO01** (аудит/решение), **T-FO02** (координатное ядро и проверки); последующие номера резервируются таблицей ниже. Поиск по существующей документации не обнаружил T-FO01/T-FO02.
Baseline: `53c86fe2` (`minor changes - before floatingorigin`). Несвязанное изменение `LiberationSans SDF - Fallback.asset` не включать.

## Статус и границы

Это новая, явно запрошенная пользователем широкая задача, а не продолжение renderer-only T-JITTER18. Старые запреты на расширение scope T-JITTER17 описывают историческое решение, не текущее поручение. T-JITTER18 остаётся отрицательным результатом; его код не возвращать.

**Полный floating origin пока НЕ внедрён.** Первый этап вводит контракты координат и проверяемое основание миграции. Пока сетевые/сценовые/физические границы не перенесены, НЕ включать ни новый сдвиг мира, ни старые `FloatingOrigin`/`FloatingOriginMP`. Успешная компиляция ядра не устраняет микротряску сама по себе.

Все Play Mode, игровые билды для приёмки и скриншоты выполняет пользователь. Автоматические проверки ограничены компиляцией, анализом исходников и чистыми Edit Mode проверками математики/сериализации без запуска сцен или физики.

Один коммит на завершённый этап: код + результаты + обновление существующего `Assets/_Project/Docs/ITERATIONS.md`. Не создавать второй журнал, не добавлять хеш своего коммита отдельным коммитом. Без push, rebase, массового git add и перезаписи scene/prefab YAML.

## 1. Подтверждённая исходная точка

- Текущий Editor: Unity **6000.5.2f1**, установленный NGO **2.13.0** (проверено через PackageInfo редактора). В предшествующем аудите URP 17.5.0; перед изменениями рендера повторно проверить пакет.
- `SceneID.SCENE_SIZE = 79999f`. Номер сцены не является физической локальной системой координат.
- Исторически подтверждённый default spawn `(39999.5, 3000, 39999.5)` уже далеко от Unity-origin; T-JITTER18 не помог на пользовательской позиции около 56.5 км.
- Рабочая архитектура использует NGO NetworkTransform, позиционные RPC и float-позиции сохранений. Существуют также отдельные deck-local и nav-sandbox координаты.
- Наличие старого класса в репозитории НЕ подтверждает его использование. Текущая задача не оживляет legacy origin-код.

Подробные доказательства, ограничения и карта границ: `01_AUDIT_AND_SOURCES.md`.

## 2. Единственная выбранная архитектура

`GlobalPosition(double X/Y/Z) → explicit LocalCoordinateFrame → Unity Vector3`.

1. **Глобальная точка** независима от origin клиента, сессии, кадра рендера и physics region. Сеть и persistence сохраняют эту точку, а не временную локальную позицию.
2. **Локальная система координат** имеет явный глобальный origin. Вычитание origin выполняется в double ДО преобразования результата в float. Не использовать `(float)global - (float)origin`.
3. **Клиенты имеют независимые origin.** Сдвиг клиента не является игровым телепортом и не требует broadcast-команды, двигающей остальные клиенты. Локальный rebase не меняет глобальный snapshot объекта.
4. **Серверные физические регионы** имеют собственные frame и PhysicsScene. Нельзя объявлять один общий origin достаточным для всех далеко разошедшихся игроков.
5. **Host** совмещает роли в одном процессе; NGO server/client экземпляр NetworkObject у хоста не становится двумя независимыми Transform. Хостовая видимая область может совпадать с её simulation frame; удалённые серверные регионы требуют изоляции физики/навигации и невидимого для камеры представления. Межрегиональные взаимодействия требуют явного handoff, не вычитания offset в случайных Update.
6. SceneID и ChunkId остаются адресами контента/интереса. Размер сетки 79999 и форматы имён сцен не менять. Физический регион существенно меньше сцены контента и не обязан совпадать с SceneID.
7. **Направления, скорости, силы, rotations, scale и локальные точки палубы** не переводить как глобальные позиции. Frame в первом ядре — только перенос, без поворота и масштаба.
8. Глобальная высота отделена от локального Y. Полный XYZ rebase допустим только после миграции altitude/cloud/fog/shader систем. Сохранение Y без сдвига не решает произвольно большую высоту.

### Сетевой контракт

- Для позиционных данных использовать явный сериализуемый global-double тип; finite/range проверки на границе.
- В первой версии не вводить агрессивную delta/half-float оптимизацию: сначала правильность. Размер точки без заголовка — 24 байта; трафик измеряется отдельно.
- Снимок движения потребует timestamp/tick, teleport generation, authority/ownership generation и parent identity. Позиция parent-relative должна иметь отдельный признак/тип, не угадываться по величине.
- Interpolation buffer хранит глобальные точки (или явно маркированный устойчивый frame), а отображение использует текущий local frame. Пакет до rebase не должен интерпретироваться в origin отправителя/получателя случайным образом.
- Инициализация, late join, pooled spawn, despawn, reconnect, parent/unparent и смена authority — обязательные части адаптера, а не последующие косметические исправления.
- Не менять NetworkBehaviour набор/порядок только на одной стороне runtime AddComponent. Изменения prefab layout должны быть одинаковыми у сервера/клиентов, через Unity API, с отдельной миграцией ассетов и проверкой.
- Не патчить `Library/PackageCache`; два notification hook NetworkTransform не являются универсальной заменой wire serialization (см. аудит исходника NGO).
- Смешанные старые/новые билды должны отвергаться согласованием версии протокола; сохранения требуют своей версии и backup. Это интеграционные задачи, не выполненные наличием структуры GlobalPosition.

### Rebase-транзакция

В будущем runtime-координатор должен работать в одной определённой фазе игрового цикла до physics/authoritative movement и сетевой выборки состояний:

1. Проверить готовность адаптеров, стабильность загрузки/спавна и полный набор зарегистрированных участников региона.
2. Подготовить новый frame и список объектов. Любой неизвестный обязательный компонент блокирует активацию, не молча исключается.
3. Перенести корни ровно один раз. Дочерние Transform автоматически следуют root; вложенные Rigidbody, CharacterController, joints и nav agents требуют согласованного snapshot/restore, а не повторного offset.
4. Обновить physics poses, interpolation history и пространственные кэши; сохранить velocities/sleep/constraints. Синхронизировать physics representation перед следующими запросами.
5. Перенести/перерегистрировать world navmesh в согласованной точке; sandbox-прокси не сдвигать автоматически вместе с игровым миром. Сохранить либо явно восстановить цели/path state.
6. Скорректировать camera lag, platform-last-position, trajectories, particles/trails/lines, pooled и inactive объекты.
7. Commit frame. Ошибка после начала изменений должна остановить симуляцию/сетевую публикацию с диагностикой; нельзя продолжать полу-сдвинутый мир.

Публичная смена глобального статического offset без транзакции запрещена. Ядро T-FO02 само ничего не перемещает.

## 3. Этапы и критерии завершения

| Тикет | Этап | Результат / gate |
|---|---|---|
| T-FO01 | Актуальный аудит, первоисточники, архитектура | Зафиксированные пути/семантика, риски Host, roadmap; без runtime изменений |
| T-FO02 | Double-coordinate foundation | GlobalPosition, immutable frame, grid bridge, чистая проверка точности и NGO сериализации; без подключения к работающим системам |
| T-FO03 | Полный migration census и spatial adapters design | Машинный список кандидатов + ручная классификация каждой границы; asset/runtime coverage; неизвестные места остаются BLOCKED |
| T-FO04 | NGO spatial replication | Собственная global-aware репликация и миграция prefab компонентов; initial sync, tick, authority, parent, reconnect; Host + 2 clients gate пользователя |
| T-FO05 | RPC/DTO и persistence | Точки боя/телепорта/докинга/штормов/FX и сохранения в global; backward-read старых сохранений, backups, protocol gate; пользовательские reconnect/save/load |
| T-FO06 | Контент и client rebase | Загрузка сцен до spawn с явным placement, DDOL/pools/static geometry/camera/shaders, independent client origins; пользовательский дальний Idle и streaming |
| T-FO07 | Серверные регионы и physics routing | PhysicsScene + отдельное управление регионами, scene-aware запросы, ship/passenger/crew/joints handoff; два далёких клиента на Host |
| T-FO08 | NavMesh, AI, корабли и остальные механики | World/deck/proxy адаптеры, cached goals, combat/docking/cruise/altitude/weather/VFX/UI; полный регрессионный прогон |
| T-FO09 | Приёмка и включение по умолчанию | Результаты пользователя, профилирование, packet loss/late join, повторные shift, restart; только после этого объявлять jitter fixed |

Номера — план, не заявление о выполнении. Подготовительные изменения можно выполнять без игровых тестов, если они не меняют рабочий runtime. Переход через интеграционный runtime gate допускается только с результатами пользователя. Пилот одного близкого клиента не считается завершением MMO-варианта.

### Фактическое продвижение на 2026-09-09

- T-FO01: завершён, коммит `ba56bdc4`.
- T-FO02: завершён как изолированное ядро, коммит `d3fb5511`; 22 исходные проверки пройдены. В T-FO03 добавлен guard extreme float translation и 23-я regression проверка.
- T-FO03: scanner и машинный census выполнены (639 C#, 48 shader files, 75 prefab assets, Bootstrap); первичная классификация 16 RPC и docking/NPC DTO сохранена. Полный semantic/closed-world-scene gate ещё открыт. Отчёт `03_BOUNDARY_REVIEW.md`.
- T-FO04A: реализованы versioned global/parent-local motion snapshots и bounded receive/interpolation buffer. Pure Edit Mode checks: 33 новых + 23 foundation, все PASS. Отчёт `04A_NETWORK_PROTOCOL_AND_BUFFER.md`. NGO transport, выдача generations и prefab/scene integration ещё не подключены; это НЕ полный T-FO04.
- T-FO04B: реализованы explicit session/generation issuer, reliable control + server admission и настоящий NGO GlobalMotionReplicator. Компонент не установлен на префабы/сцены и не пишет Transform. 32 новых + 56 прежних pure проверок = 88 PASS. Отчёт `04B_NGO_TRANSPORT.md`. Живые сетевые сессии не тестировались.
- T-FO04C: реализованы GlobalMotionWorld (session/frame registry), GlobalMotionPoseAdapter и pure application policy. Новые компоненты не установлены на сцены/префабы. Compile PASS; 32 новых + 88 прежних pure проверок = 120 PASS / 0 FAIL. Отчёт `04C_FRAME_POSE_ADAPTER.md`. Native lifecycle/physics и gameplay не тестировались; unsupported nav/joints/nested bodies и interpolated-parent server replica блокируются.
- T-FO04D: добавлены dormant hooks в player/ship/NPC/skills и отдельные social/NavTick пути; opt-in по умолчанию false, baseline placement отделён от actor/native readiness. 26 новых + 120 прежних pure проверок = 146 PASS / 0 FAIL, compile PASS. Отчёт `04D_ACTOR_READINESS.md`. Текущие префабы/сцены не изменены, native preparation/полный runtime gate не реализованы.
- T-FO04E: завершена dormant подготовка startup/layout contracts: explicit prefab SHA256 + выбранный PlayerPrefab, strict 72-byte hello, scoped approval gate и interface заранее подготовленного spawn/parent bootstrap. Профиль-ассет не создан; scenes/prefabs не редактировались. Compile PASS; **48 новых + 146 прежних = 194 pure PASS / 0 FAIL**. Read-only audit: 58 candidate prefabs, opt-in=0, profile assets=0; это не полный scene census. Отчёт `04E_NETWORK_STARTUP_CONTRACTS.md`, результаты `04E_STATIC_VALIDATION.json` и `04E_PREFAB_CONTRACT_AUDIT.json`.
- T-FO04F: реализовано limited custom hierarchy placement из нового trusted baseline; prospective parent/pose preflight до публикации control, actual SetParent + pose до callbacks/ACK. Same-binding motion hierarchy не меняет. Cross-scene/frame, циклы, Rigidbody/joints/произвольные colliders/enabled NavMeshAgent changes блокируются; CC допускается в ограниченной конфигурации, native rollback отсутствует. Global protocol=0xF002, старый 0xF001 reserved/incompatible. Compile PASS; **40 новых + 194 прежних = 234 pure PASS / 0 FAIL**. Отчёт `04F_CUSTOM_HIERARCHY_BASELINES.md`, результаты `04F_STATIC_VALIDATION.json`. Текущая игра не активирована и runtime parenting не проверен.
- T-FO04G: реализован dormant concrete **player-only** GlobalMotionPlayerBootstrap: approved-peer tickets, delayed registration подготовленных frames, typed global pre-instantiation seed через NGO 2.13 handler, local pose/scene до Awake, PostSpawn Bind/first World baseline и отдельный initial CC latch. Первый server spawn синхронный и ограничен root NetworkPlayer/CC; general native async readiness, pools и scene-object lifecycle не реализованы. Source prepared content/global persistence остаётся обязательным интерфейсом **без реализации**, startup блокируется без него; активный legacy ClientSceneLoader не допускается. Global protocol=0xF003. Compile PASS; **44 новых + 234 прежних = 278 pure PASS / 0 FAIL**, opt-in/profile assets/loaded profiles/adapters=0. Отчёт `04G_PLAYER_SPAWN_BOOTSTRAP.md`, результаты `04G_STATIC_VALIDATION.json`. Сцены/префабы/профиль не изменялись; настоящий prefab/NGO/CC runtime не проверен.
- T-FO04H: реализованы strict compiler/digest для **заявленного reviewed scene catalog**, immutable parent-first/reverse plan, lifecycle receipt ledger (load generations, parent/frame order, exact retirement tokens для network и обычного content), read-only scene audit. Profile hello теперь связывает scene digest с каталогом и проверяет replacement prefab hash/role; protocol=0xF004. Compile PASS; **56 новых + 278 прежних = 334 pure PASS / 0 FAIL**. Реальный candidate audit: 26 сцен, 1 loaded/dirty, 25 uninspected, 61 Unreviewed root/NetworkObject entries, 3 missing-component subtree diagnostics; catalog assets=0. Это НЕ утверждённый closed-world catalog и НЕ native spawn/retire executor. Отчёт `04H_SCENE_CATALOG_AND_LIFECYCLE.md`, результаты `04H_STATIC_VALIDATION.json` / `04H_SCENE_CATALOG_AUDIT.json`. Существующая untracked schema GlobalMotionSceneCatalog.cs сохранена без переписывания; сцены/префабы не изменялись.
- T-FO04I: реализованы dormant baked-marker binding и ограниченный GlobalSceneNativeExecutor: pre-start placement уже inactive static content, post-sweep server Spawn NONSPATIAL scene services, client activation до NGO scene lookup, публичный Additive sync и remote admission после receipts. Требуются весь catalog preloaded на каждом peer, marker каждого observed root/NetworkObject, явные frames/poses/activation intent; arbitrary active gameplay не отключается. Child-first local retire выполняет read-only receipt preflight → actual Despawn/deactivation → receipt; native partial failure не откатывается. Spatial scene actors/body/nav/CC, replacement/exclusion, DDOL и distributed streaming НЕ реализованы. Protocol=0xF005. Compile PASS; **31 новых + 334 прежних = 365 pure PASS / 0 FAIL**. Loaded markers/executors/profile/catalog assets/opt-in=0; прежние audit blockers H сохранены. Отчёт `04I_NATIVE_SCENE_EXECUTION.md`, результаты `04I_STATIC_VALIDATION.json` / `04I_SCENE_CATALOG_AUDIT.json`. Native executor фактически не вызывался, runtime UNTESTED; сцены/префабы не изменялись.
- T-FO05A: подготовлена fileless зависимость global persistence для G source, без подключения к runtime. Immutable player checkpoint с явной persistent identity/global double/ship affinity/timestamp, отдельный versioned canonical JSON v1 + checksum; read-only импорт текущего известного legacy wrapper требует reviewed source-text digest, полной identity mapping и явной absolute/local-frame provenance. Нет persisted runtime frame/clientId, угадывания origin, потери ship payload или изменения legacy DTO/repository. Frozen v1/legacy fixtures. Compile PASS; **69 новых + 365 прежних = 434 pure PASS / 0 FAIL**. Отчёт `05A_PLAYER_GLOBAL_PERSISTENCE.md`, результаты `05A_STATIC_VALIDATION.json`; actual saves не читались/не писались. Это НЕ repository/backup/restore/provider integration и НЕ прохождение T-FO04 gate.
- T-FO05B: реализованы dormant immutable all-player snapshot envelope, transactional repository и конкретный Directory storage adapter. Explicit directory/store ownership, cooperative lease, CreateNew pending + Flush(true), same-directory File.Replace/Move без copy/delete fallback; previous backup, explicit corrupt-primary/pending quarantine и recovery с новым commit ID. Точные observation fingerprints/lineage, stale-write/ABA guards, разрешение удалений offline players и проверка старых timestamps. Compile PASS; **68 новых + 434 прежних = 502 pure PASS / 0 FAIL** на in-memory fault model. Native disk adapter НЕ вызывался; real saves/backups/FS crash/reconnect/IL2CPP UNTESTED. Отчёт `05B_CHECKPOINT_TRANSACTIONS.md`, результаты `05B_STATIC_VALIDATION.json`. A/legacy/runtime код не изменялся, repository к игре не подключён.
- T-FO05C: реализован dormant конкретный NGO source для accepted WORLD/on-foot checkpoint, explicit trusted identity leases по реальным connection/player/session/run/spawn refs; readonly World/Replicator seams читают latest _serverControl, не render pose. Immutable issued point-in-time evidence допускает свежую более новую обычную motion sequence, но отвергает scope/identity/authority/discontinuity changes. C coordinator сохраняет offline records и вызывает B CAS с guard до/после staging, single-use batches и явным Pending/Indeterminate handling без blind retry. Compile PASS; **58 новых + 502 прежних = 560 pure PASS / 0 FAIL**. Native source/disk/реальная сеть НЕ вызывались, auth/provider/restore integration отсутствуют. Отчёт `05C_AUTHORITATIVE_CAPTURE_AND_MERGE.md`, результаты `05C_STATIC_VALIDATION.json`; A/B formats и protocol F005 неизменны, игра не активирована.
- T-FO05D: реализован concrete GlobalMotionCheckpointSpawnSource для fixed prepared frames: явная pre-spawn identity lease по actual connection/session/run, checkpoint lookup либо явно заданный first-spawn, pose/rules/frame/deadline и nonce; G factory проверяет reservation до создания, после Awake/OnEnable и подтверждает actual-ready player с handoff к C. B имеет readonly observation fence, C — callback-safe retirement; late cancel не отзывает completed capture identity. Ни frame/AOI loader, ни auth input/orchestration не выдумываются, I native content validation сохранена. Compile PASS; **60 новых + 560 прежних = 620 pure PASS / 0 FAIL**. Native source/spawn/disk не запускались; loaded source/profile/opt-in=0, scenes/prefabs не менялись, F005/A/B schemas сохранены. Отчёт и анализ остатка: `05D_CHECKPOINT_SPAWN_SOURCE.md`; результаты `05D_STATIC_VALIDATION.json`.
- T-FO05E: реализован GlobalMotionSessionCoordinator и подключён к actual global-profile ветке NMC: explicit prepared frames/repository/Host policy, session-token + actual NetworkClient trusted receipts, deferred D preparation/confirmed lifetime, C/B checkpoint cadence с backoff/fault latch, checkpoint-before-deliberate-stop либо explicit abandon. G/D confirmation/cancel seams расширены, global NetworkPlayer не ждёт legacy restore. NMC пропускает legacy persistence/reset только в global paths, E блокирует любые loaded legacy position services (даже disabled из-за Awake subscriptions). EscMenu получает blocked callback и не зависает; global teardown timeout не выдаёт success. Compile PASS; **58 новых + 620 прежних = 678 pure PASS / 0 FAIL**. Реальные auth/NGO/native GUI/disk не вызывались; loaded session/source/profile/opt-in=0. Отчёт `05E_SESSION_ORCHESTRATION.md`, результаты `05E_STATIC_VALIDATION.json`.
- T-FO06A: завершён **read-only pilot preflight**, не конфигурация пилота. Выбранный canonical NetworkPlayer имеет 4 группы несовместимостей (нет replicator/opt-in adapter, есть stock NT, NO flags); Bootstrap остаётся dirty с 3 unresolved missing component entries, двумя legacy position services и активным legacy ClientSceneLoader. WorldScene_0_0 подтверждена только по asset/dependency metadata, hierarchy UNINSPECTED. NGO auto-generation=true: новый root-NetworkObject prefab автоматически попадает в активный default list независимо от папки; список из 58 entries не менялся. Registration не означает spawn/активацию. Три integrity assertions PASS, JSON round-trip complete, No compile errors; 678 прежних pure checks не перезапускались. Отчёт `06A_PILOT_SCOPE_PREFLIGHT.md`, baseline JSON и воспроизводимый `tools/AuditFo06APilot.cs` вне Assets. Native сцены/префабы/settings/runtime не менялись, Play Mode не запускался.
- T-FO06B: prefab registration policy переведена в explicit режим — `GenerateDefaultNetworkPrefabs=false`, сохранено в `ProjectSettings/NetcodeForGameObjects.asset`; `DefaultNetworkPrefabs.asset` остался 58 entries, bytes/meta идентичны, canonical player по-прежнему listed. Создан isolated pilot `Assets/_Project/Prefabs/FloatingOrigin/NetworkPlayer_GlobalPilot.prefab` как **независимая копия** (не variant), чтобы override не мог вернуть stock writer; цена — визуальные правки canonical не наследуются. Удалён NetworkTransform, добавлены PlayerAttacker/PlayerTarget/Replicator/PoseAdapter с `_coordinatesRequired=true`, пять NO flags = false. Уточнение к 06A: ValidateLayout содержит **пятое** требование (baked combat behaviours), согласованное с существующей runtime-проверкой NetworkPlayer; SkillInputService/SkillAnimationPlayer/Passthrough — обычные MonoBehaviour и порядок NetworkBehaviour не меняют. Верификация: Spatial contract PASS, pilot hash отличен от canonical, canonical остался legacy, 8 protected файлов побайтово равны 06A, автогенерация off, registry=58, pilot не зарегистрирован и не выбран, No compile errors. Пилот попадает под `.gitignore` `*.prefab` и в коммит не входит: публикация требует отдельного решения. Отчёт `06B_PILOT_PLAYER_PREFAB.md`, JSON и `tools/VerifyFo06BPilotPrefab.cs`.
- T-FO06C: пилотный префаб переведён под Git узким исключением в `.gitignore` (папка `Assets/_Project/Prefabs/FloatingOrigin` + `.meta` для стабильных GUID). Общее правило `*.prefab` сохранено, поэтому остальные префабы и сцены остаются за Lore VCS; проверено `git check-ignore`, что canonical player и BootstrapScene по-прежнему игнорируются. Повторная верификация после правки: contract PASS, состояние пилота и 8 protected файлов без изменений, No compile errors. Отчёт `06C_PILOT_PREFAB_TRACKING.md`.
- T-FO06D: выбран путь сокращения. Создан минимальный `Assets/_Project/Prefabs/FloatingOrigin/GlobalPilotNetworkPrefabs.asset` — 1 запись (пилотный игрок), `Override=None`, `IsDefault=false`, ни одной ссылки из сцен; default-реестр остался 58 entries, bytes идентичны, пилот в него не попал. Доказательная классификация всех 58: **Spatial-ready=0, NonSpatial-ready=1 (`NEWCHESTPREFAB`), требуют миграции=57**; причины — 54 `missing_global_motion_components_or_optin`, 2 `invalid_behaviour_count` (`TestPlayer`, `PickupItem_Test`), 2 `inactive_network_behaviour_object` (`NetworkChestContainer_Test`, `SPAWN_TEST`). Эти 4 записи не проходят **ни одну** роль, что делает путь полного реестра блокированным структурно, а не только по объёму. Вне пилота остаются спавн кораблей/NPC/предметов/сундуков/ресурсов/зон, legacy position services, ClientSceneLoader, 3 missing scripts, WorldScene и issuer/store. `.gitignore` расширен на `*.asset.meta` в пилотной папке. Отчёты `06D_PILOT_REGISTRY_AND_REMAINDER.md`, `06D_REGISTRY_CLASSIFICATION.json`, `tools/AuditFo06DRegistryClassification.cs`. No compile errors.
- T-FO06E: `BootstrapScene.unity` и её `.meta` переведены под Git точечными исключениями по полным путям; общее правило `*.unity` сохранено, копия сцены остаётся игнорируемой. В коммит вошла версия файла с диска: в редакторе сохраняются несохранённые изменения, предшествующие этой работе, сцена не сохранялась. **Поправка к 06C/06D:** утверждение «остальные префабы и сцены за Lore VCS» было неточным — `git ls-files` показывает, что `WorldScene_0_0.unity` и 43 префаба уже отслеживаются, поскольку правила игнорирования добавлялись позже и на tracked-файлы не действуют. Зафиксирована развилка применения пилотного реестра: эффективный реестр = все списки + встроенные записи, поэтому пилотный список должен заменять default. Вариант A (замена в сцене) ломает спавн всех 58 префабов в legacy-режиме; вариант B (условная подмена только при назначенном профиле) сохраняет текущую игру; вариант C — отдельная bootstrap-сцена с дублированием. Рекомендован B, ни один не реализован. Отчёт `06E_SCENE_TRACKING_AND_REGISTRY_FORK.md`.
- T-FO06F: реализован выбранный вариант **B**. Профиль получил декларативное `_registryLists`; подмена выполняется только над живой конфигурацией в global-ветке, до сборки hello и снятия хеша, поэтому защита `bootstrap_changed_network_start_ownership` продолжает проверять именно вмешательство bootstrap. Восстановление дословно возвращает исходный экземпляр списка через `Release` и через `finally` на путях отказа; владение передаётся gate в момент создания, двойного восстановления нет. Ключевая деталь: `Prefabs.Prefabs` — агрегированный `[NonSerialized]` кэш, в редакторе уже заполненный из default-списка, поэтому после подмены и после восстановления вызывается `Prefabs.Initialize()` — иначе эффективный реестр сохранил бы старые 58 записей. Принятое последствие: `Initialize` подписывает `OnAdd`/`OnRemove` без отписки, повторная подписка возможна, но обработчики срабатывают лишь при рантайм-изменении списков. Валидация состава вынесена в чистый `GlobalMotionNetworkContract.ValidateRegistryComposition`. Добавлен `ValidateGlobalRegistryComposition` — 13 чистых проверок без GameObject/NetworkManager/сцен. Прогон всех валидаторов: **16 из 16, 691 PASS / 0 FAIL** (678 прежних + 13 новых), No compile errors. Фактическая подмена в реальном запуске не проверялась: профиль не создан. Отчёт `06F_GLOBAL_PATH_REGISTRY_SWAP.md`.
- T-FO06G: начат осмотр под каталог, потому что digest обязан совпадать с компиляцией каталога, а каталог требует осмотра сцен. Установлено жёсткое требование компилятора: источник каталога обязан иметь `inspectedComplete` **и** `saved`, иначе `missing_uninspected_dirty_or_invalid_scene_source` — каталог для несохранённой сцены невозможен. Собственный аудит проекта: 26 сцен-кандидатов, 1 осмотрена, 25 не осмотрены, 1 dirty, 61 unreviewed observation, 0 каталогов, 3 ошибки. **Опознаны три ранее UNRESOLVED компонента** по GUID из YAML плюс подтверждение в коде: `[ShipKeyToast]` `7e4592bf…` (DEPRECATED), `[ShipKeyServer]` `d915eb76…` (заменён MetaRequirementRegistry), `[ShipOwnershipRegistry]` `75a1a615…` (удалён как дублирующий KeyRodInstanceWorld). Классы отсутствуют в коде, GUID отсутствуют во всех `.meta` — это мёртвые ссылки на намеренно удалённые классы, а не потерянная функциональность; удаление не выполнялось. Исправлена ложная диагностика: 18 из 21 срабатывания были встроенными объектами движка (`fileID 19102`), учитывается только `fileID 11500000`; итог 3 совпадает с 3 ошибками аудита. Блокеры: несохранённые изменения сцены и связанная с ними невозможность убрать мёртвые компоненты без сохранения всего состояния. Отчёт `06G_SCENE_REVIEW_AND_MISSING_SCRIPTS.md`, `06G_MISSING_SCENE_SCRIPTS.json`, `tools/DiagnoseMissingSceneScripts.cs`.
- T-FO06H: пользователь сохранил сцену; выяснилось, что несохранённые изменения были безобидны — Unity дописала три поля `_globalMotionProfile`/`_globalSpawnBootstrap`/`_globalSessionCoordinator` со значением `fileID: 0`, посторонней работы там не было. Из `BootstrapScene` удалены три мёртвых компонента через штатный `RemoveMonoBehavioursWithMissingScript` ровно на трёх опознанных объектах. Diff подтвердил опознание через `m_EditorClassIdentifier`: `ProjectC.Ship.Key.ShipKeyToast`, `ProjectC.Ship.Key.ShipKeyServer`, `ProjectC.Ship.Network.ShipOwnershipRegistry`. Проверено: missing `3 → 0`, количество GameObject и корней без изменений, компонентов ровно `−3`, сцена не dirty, ни один GUID в файле не найден; diff — только 3 блока MonoBehaviour и 3 ссылки в `m_Component`. После сохранения Unity перестала отвечать инструментам, хотя процессы живы и лог показывает успешный импорт без ошибок; проверка компиляции отложена до восстановления связи. Отчёт `06H_DEAD_COMPONENT_CLEANUP.md`.
- T-FO06I: область пилота зафиксирована пользователем — только `BootstrapScene` и `WorldScene_0_0`; остальные 24 сцены содержат лишь разметку границ и в пилот не входят. Измерен объём каталога по сохранённому YAML: Bootstrap 222 GO / 55 корней / 18 NO → 60 observations; WorldScene_0_0 1191 GO / 26 корней / 23 NO → 35 observations; **итого 95 проверенных записей**, а не 1413 — записи нужны только корням и NetworkObject'ам. Расхождение с прежним аудитом (61 против 60, 56 против 55 корней) объясняется способом подсчёта; источником авторинга остаётся собственный аудит проекта, использующий `GlobalObjectId`. **Найден четвёртый мёртвый компонент** — `[KeyRod_ShipHeavy]` в WorldScene_0_0, guid `41bc0fc7…`, класс `ProjectC.Ship.Key.KeyRodInstanceBinding`, намеренно удалённый («KeyRodInstanceBinding удалён» в PickupItem.cs; InventoryUI защитно резолвит тип и работает без него). Не удалён: сцена не загружена. В Bootstrap отсутствующих скриптов теперь 0. Отчёт `06I_PILOT_CATALOG_SIZING.md`, `06I_PILOT_CATALOG_SIZING.json`, `tools/SizePilotSceneCatalog.cs`.
- T-FO06J: `WorldScene_0_0` загружена дополнительно, обе сцены осмотрены живьём. **Оценка 06I исправлена:** корней в WorldScene_0_0 58 (не 26), мёртвых компонентов 3 (не 1), observations **150** (не 95). Причины — prefab-инстансы не видны в YAML сцены как обычные корни, и два мёртвых компонента не имеют GUID вовсе. Опознаны: два `Assembly-CSharp-Editor::ProjectC.Editor.PoleBlockerComponent` (класс отсутствует нигде, к тому же Editor-сборка неработоспособна в рантайм-сцене) и один `KeyRodInstanceBinding`. **Блокер:** удалить их публичными API Unity невозможно — `RemoveMonoBehavioursWithMissingScript` не считает слот без ссылки сломанным, а `m_Component` не даёт ни `DeleteArrayElementAtIndex`, ни изменения `arraySize`. Все попытки прерваны до сохранения, обе сцены побайтово совпадают с диском. Это блокирует каталог: аудит выставляет `inspectedComplete = false` при ошибке в поддереве наблюдаемого корня, а компилятор такой источник отвергает. BootstrapScene готова: ошибок осмотра 0, безобидный флаг dirty сброшен сохранением идентичного содержимого (байты не изменились); установлено, что флаг поднимает перезагрузка домена, а не правки. Отчёт `06J_LIVE_SCENE_REVIEW.md`.
- T-FO06K: мёртвые ссылки убраны пользователем вручную; фактический diff шире трёх компонентов — удалено 15 объектов, включая `Boundaries_0_0`, `GroundPlane_0_0` (локально `39999.5/0/39999.5`, под исторической точкой спавна) и весь `[KeyRod_ShipHeavy]`. Земля и границы были соседями, а не вложенными, поэтому это два отдельных удаления; удаление земли задокументировано как риск для пилота. Оба блокера каталога снялись: missing-компонентов 0, ошибок осмотра 0, флаги dirty сброшены без изменения байтов, observations 150. **Главный результат: каталог не покрывает эти сцены.** Из 150 наблюдений поддержано лишь 58, не поддержано 92; по сценам — Bootstrap 54/7, WorldScene_0_0 4/85. Политика допускает только статичный контент и неспатиальные сетевые сервисы: `Exclude`/`ReplaceWithNetworkPrefab` дают `replacement_and_exclusion_need_extended_executor`, spatial+NetworkObject — `spatial_scene_network_actor_bridge_missing`, а поддерево spatial-записи ограничено Transform/MeshFilter/Renderer/Collider/маркером. Поэтому камеры, свет, VFX, UI, NPC и корабли не классифицируются никак. Семь проблемных наблюдений Bootstrap: `Clouds`, `PlayerSpawner`, `MainCamera`, `Sun`, `Moon`, `TestObjects`, `ConstellationController`. Требуется развилка контракта: **вариант A** — добавить проверенную категорию «не управляется» (правка treatment/компилятора/политики/исполнителя и подъём версии протокола, снимает блокировку пилота), **вариант B** — расширить исполнителя до spatial-сетевых актёров (объём T-FO07/T-FO08). Рекомендован A, ни один не реализован. Отчёт `06K_CATALOG_FEASIBILITY_LIMIT.md`.
- Следующий ближайший фокус: подтвердить вариант расширения контракта; решить судьбу удалённой `GroundPlane_0_0`; затем каталог на 150 записей (treatment/reviewNote/spatial/позы) (25 world-сцен в него входить не обязаны) и составить каталог только для него; затем profile с каталогом из одной Spatial-записи, ссылкой на пилотный список в `_registryLists` и корректным SceneLayoutDigest; классификация профиля/каталога (эффективный registry должен точно совпадать с classified catalog, а не только игрок), согласование dirty Bootstrap/missing scripts/legacy service ownership и реальный native audit WorldScene. Ранее выполнено: согласовать **явную prefab registration policy** перед созданием isolated global player variant; proposed disable auto-generation сохраняет текущие 58 legacy entries, но меняет будущую регистрацию сетевых prefab и требует отдельного подтверждения. Затем — variant/layout и отдельный pilot list, сохранность canonical visual/CC config, согласование dirty Bootstrap/missing scripts/service ownership и реальный native audit WorldScene. Порядок и ограничения — §6 `06A_PILOT_SCOPE_PREFLIGHT.md`. Trusted account issuer/его реальные вызовы, catalog/markers/frames/physics, profile и explicit store/configuration всё ещё необходимы. Остаются AOI/streaming, ship/ParentLocal composition, RPC/DTO, individual disconnect final-save/respawn, client rebase, multi-region physics/Host, NavMesh/AI, visual/global-height consumers и user acceptance. Полные T-FO04–09 и semantic T-FO03 не закрыты; world shift выключен, jitter fixed не заявляется.
- Оценка после D (подробно §8 отчёта): примерно **4–6 этапов до ограниченного fixed-frame Host+client прогона**, **8–12 до первого настоящего rebase-прогона**, **20–35 сопоставимых итераций до полной интеграции по 10 крупным блокам**, плюс ориентировочно 2–3 пользовательских цикла проверки/исправления. Диапазоны включают предыдущие рубежи, не суммируются; не календарная гарантия. Unknown scene/native scope и результаты первых тестов могут увеличить оценку. 620 pure checks не означают процент runtime готовности.

## 3.1 Актуализация статуса на 2026-09-11

Главный план сохраняет правильную последовательность: pure contracts и read-only boundary audits → user-controlled runtime evidence → concrete adapters → только затем `Apply/Rebuild/Validate/Publish` и полноценная rebase transaction. Исторические пункты выше не переписываются.

- `T-FO06N`: design-only closed-world participant set и atomic transaction contract; затем compile-verified coordinator/preflight/snapshot skeleton. Coordinator останавливается на `Captured`.
- `T-FO06O–P`: read-only participant identity census и deterministic candidate mapping; owner-reviewed final manifest ещё не опубликован.
- `T-FO06Q`: source-level camera/ShipDeckNav/NGO boundary audit; runtime ordering и ownership остаются INCONCLUSIVE.
- `T-FO06R`: catalog/NetworkObject policy cross-audit; `96/96` matches, automatic admission `0`.
- `T-FO06S`: fail-closed participant admission policy; live manifest publication отсутствует.
- `T-FO06T`: dormant seven-part runtime-proof contract; user runtime evidence отсутствует.
- `T-FO06U`: user log подтверждает initial startup/native preparation, но фиксирует повторные NavMesh agent failures; deck readiness и passenger provenance INCONCLUSIVE.
- `T-FO06V`: pure ShipDeckNav/NavMesh readiness contract; `15/15` pure checks PASS, runtime registration и completed passenger attachment не доказаны.
- `T-FO06W`: пакет camera ownership/history и Unity-state rollback contracts; `35/35` pure checks PASS, runtime camera/rollback evidence не доказано.
- `T-FO06X`: новый пользовательский 20-секундный Host capture повторяет static native/player startup PASS и фиксирует визуально сохраняющийся jitter. `20` ShipDeckNav registrations и `20` crew attachment/spawn sequences наблюдались, но повторные NavMesh agent failures сохраняют deck/passenger evidence в статусе INCONCLUSIVE. Camera/history, NGO/physics ordering, post-rebase baseline и rollback не записаны; `admitted=0`.
- `T-FO06Y`: подготовлен dormant user-controlled runtime evidence probe для isolated global pilot. Read-only hooks различают player/CharacterController/grounding/platform carry, Animator root-motion state, NGO tick/control/baseline, physics `SyncTransforms` и independent camera ownership/history. Probe добавлен только в pilot prefab и выключен по умолчанию; compile PASS.
- `T-FO06Y runtime follow-up`: пользовательский export от 2026-09-11 18:49:19 содержит `768` записей за примерно `15 секунд`, но `0` строк `[T-FO06Y]`. Static native/player startup повторно PASS; `20` ShipDeckNav registrations и `20` crew attachment/spawn sequences наблюдались, однако повторные NavMesh agent failures сохраняют deck/passenger evidence в статусе INCONCLUSIVE. Movement, Animator, NGO/physics ordering, camera/history, post-rebase baseline и rollback остаются UNVERIFIED; `admitted=0`.
- `T-FO06Y runtime follow-up 02`: второй export от 2026-09-11 18:53:31 содержит `805` записей за `15 секунд` и снова `0` строк `[T-FO06Y]`. Static native/player startup PASS, `20` ShipDeckNav registrations и `20` crew attachment/spawn sequences наблюдаются, но NavMesh failures сохраняются. Дополнительно зафиксирован `The referenced script (Unknown) on this Behaviour is missing!`; его причинная связь с jitter не доказана. Runtime proof и admission остаются закрытыми.
- `T-FO06Y runtime follow-up 03`: третий export от 2026-09-11 18:56:49 содержит `1319` записей; `[T-FO06Y]` evidence присутствует в окне `frame=2958..3324`, `fixedTime=44.5200..51.9800`. Binding стабилен (`5641052402999880809/94/1/1/2`), `adapter=Ready`, `baselinePlaced=True`. Подтверждены player/CharacterController, grounding, Animator, NGO ticks/control, camera owner и camera history. Одновременно отсутствуют `Physics.SyncTransforms`, `baseline.AcknowledgeApplied`, `baseline.ActorApplied`, controlled movement/rebase и rollback evidence. Повторные `FixedUpdate`/несколько `NetworkTick` в одном frame фиксируются как scheduling/order anomaly с неопределённой причиной; NavMesh passenger gate остаётся `INCONCLUSIVE`. Runtime proof и admission остаются закрытыми. Полный отчёт: `06Y_RUNTIME_CAPTURE_FOLLOWUP_03.md/.json`.
- `T-FO06Y source analysis 04`: read-only анализ исходников probe/adapter/replicator/camera показал, что `AcknowledgeApplied` пишется только при переходе baseline acknowledgement, `ActorApplied` — только для нового binding, а `Physics.SyncTransforms` — только при фактическом `WritePose`. Поэтому их отсутствие в idle capture совместимо с baseline, выполненным до evidence window, либо отсутствием нового binding; точный момент baseline остаётся INCONCLUSIVE. `camera.LateUpdate.skip` до захвата target/locked cursor соответствует исходному guard. Runtime scheduling anomaly и NavMesh passenger readiness остаются INCONCLUSIVE. Полный отчёт: `06Y_RUNTIME_EVIDENCE_SOURCE_ANALYSIS_04.md/.json`.
- `T-FO06Y runtime capture gate 05`: подготовлен следующий serial user-controlled capture gate. Требуются baseline transition с `ActorApplied`, `AcknowledgeApplied` и `Physics.SyncTransforms`, затем movement/jump, controlled rebase/post-rebase continuity, rollback и отдельная passenger/deck readiness проверка без NavMesh blocker. Runtime gates остаются fail-closed. Обнаруженное незакоммиченное изменение isolated pilot prefab (`_captureEnabled=1` и новый `GlobalObjectIdHash`) не включалось и требует отдельного user review. Полный отчёт: `06Y_RUNTIME_CAPTURE_GATE_05.md/.json`.
- `T-FO06Y runtime follow-up 04`: точечный Unity MCP анализ активного Play Mode подтвердил новый baseline на frame `271` с `Physics.SyncTransforms`, `ActorApplied` и `AcknowledgeApplied`, стабильный binding `5044784805265246929/94/1/1/2`, jump, controlled movement и post-movement stable state. Rebase, rollback, admission, manifest и passenger/NavMesh readiness не подтверждены; camera после примерно frame `597` пишет `skip(... cursor=None)`. Полный отчёт: `06Y_RUNTIME_CAPTURE_FOLLOWUP_04.md/.json`.
- `T-FO06Z`: добавлен dormant read-only ShipDeckNav/passenger evidence adapter поверх существующего T-FO06Y probe. Runtime records теперь различают registration, валидность native `NavMeshDataInstance`, proxy creation/`isOnNavMesh`, explicit attachment и ship identity. Compile PASS; Play Mode и runtime mutation не выполнялись. Отчёт: `06Z_SHIP_DECK_RUNTIME_EVIDENCE_ADAPTER.md/.json`.
- `T-FO06Z runtime follow-up 01`: пользовательский Play Mode capture проверен точечно через Unity MCP. Подтверждены `decks=20` с `reg=True;instance=True;ready=True`, `passengerCount=20` с `active=True;proxy=True;onNav=True;navActive=True`, observed revisions `1→6`, `adapter=Ready`, `baselinePlaced=True`, post-rebase `grounded=True;ccGrounded=True` и фактическое движение игрока. Повторные `Failed to create agent because it is not close enough to the NavMesh` сохраняются как transient startup warning; фильтр `navActive=False` был inconclusive из-за MCP timeout. `runtimeProofComplete=false`, `runtimeAdapterReady=false`, `rollbackReady=false`, `admittedParticipants=0`, `liveManifestPublication=false`; runtime rebase readiness остаётся `NOT READY`. Отчёт: `06Z_RUNTIME_CAPTURE_FOLLOWUP_01.md/.json`.
- `T-FO06Z runtime follow-up 02`: после пользовательского движения, прыжка и остановки Play Mode точечный MCP review подтвердил baseline (`SyncTransforms`, `ActorApplied`, `AcknowledgeApplied`), movement, instrumented jump (`velocity.y=6.07/5.21` при `grounded=False`), возврат `grounded=True`, стабильный binding `5038395829736550539/94/1/1/2`, sampled revisions `1,2,20,21` и `20/20` deck/passenger readiness. Explicit rebase markers и rollback/shutdown markers не найдены; поздняя camera evidence содержит `LateUpdate.skip(cursor=None)`, поэтому camera continuity и полный rebase gate остаются `INCONCLUSIVE`. Повторные NavMesh warnings сохранены. Play Mode остановлен, `hasCompilationErrors=false`; runtime rebase readiness остаётся `NOT READY`. Отчёт: `06Z_RUNTIME_CAPTURE_FOLLOWUP_02.md/.json`.
- `T-FO06Z source audit`: прямой read-only поиск по coordinator/probe/rollback boundary подтвердил, что `GlobalMotionRebaseCoordinator` останавливается на `Captured`, а `Apply/Rebuild/Validate/Publish` не подключены. Интегрированный runtime trigger и ordered rebase/rollback markers в проверенных исходных областях не найдены; `UnityStateRollbackContract` остаётся только validator и не захватывает/не восстанавливает Unity state. Результат ограничен audited paths, поскольку изолированный project-audit завершился `network error`. Runtime rebase readiness остаётся `NOT READY`. Отчёт: `06Z_SOURCE_AUDIT_AND_TRIGGER_ABSENCE.md/.json`.
- `T-FO06Z transaction boundary`: добавлен runtime-independent `GlobalMotionRebaseTransactionContract` с fail-closed `UserControlled` trigger validation, ordered фазами `Requested → FramePrepared → Applied → PhysicsSynchronized → Validated → Published → Completed` и отдельными `RollbackBegun → Restored → RollbackCompleted`. Контракт не подключён к coordinator и не изменяет Unity state; automatic threshold trigger, native rollback и runtime markers не добавлялись. Compile PASS (`No compile errors`). Отчёт: `06Z_REBASE_TRANSACTION_BOUNDARY_CONTRACT.md/.json`.
- `T-FO06Z runtime follow-up 03`: после остановки Play Mode pointwise MCP review подтвердил baseline, стабильный binding, post-placement movement, `20/20` deck/passenger readiness и отсутствие compile errors. Одновременно зафиксирована initial placement/writer anomaly: игрок падает с `y=1` до `y=-7.41`, затем без explicit rebase markers оказывается на `y≈2502.77`; repeated FixedUpdate/NetworkTick scheduling остаётся INCONCLUSIVE. Фильтры `rebase` и `rollback` дали `0`; текущий runtime driver отсутствует. Повторять идентичный capture до появления driver больше не нужно. Следующий этап — implementation/debugging slice для placement/writer boundary и reviewed runtime driver с ordered markers/native rollback. Отчёт: `06Z_RUNTIME_CAPTURE_FOLLOWUP_03.md/.json`.
- `T-FO06AA initial global spawn gate`: устранено окно до baseline, в котором `GlobalMotionActorLink.Required` ещё не был поднят через `RecordBaseline`, а `NetworkPlayer` уже мог выполнять legacy `FixedUpdate`/CharacterController gravity. `GlobalMotionSpawnLatch` теперь явно armed до активации клона, сохраняет armed-состояние через `OnNetworkSpawn`, блокирует simulation до точного baseline release и пишет `spawn.InitialGateArmed`, `spawn.NetworkSpawn`, `spawn.InitialGateReleased`, `player.FixedUpdate.blocked` и `spawn.FactoryPosePrepared`. Это implementation/debugging slice для anomaly `y≈1 → y≈2502`; automatic rebase trigger и native rollback по-прежнему не подключены. Compile PASS (`No compile errors`). Отчёт: `06AA_INITIAL_GLOBAL_SPAWN_GATE.md/.json`.
- `T-FO06AA runtime capture 01`: точечная проверка пользовательского Play Mode capture подтвердила `NetworkSpawn armed=True`, baseline `Physics.SyncTransforms`/`ActorApplied` до `InitialGateReleased`, но не обнаружила в доступном buffer `InitialGateArmed`, `FactoryPosePrepared` и `FixedUpdate.blocked`. Аномалия повторилась уже после release: `CharacterController.Move.after` достиг `y=-9.18`, затем `PlayerRespawnTracker` записал teleport на `y=2502.77`, а следующий frame показал игрока на `y=2502.77`. `rebase.*` и rollback markers отсутствуют; initial spawn gate не является достаточным объяснением, главным кандидатом становится respawn/correction writer path. Отчёт: `06AA_RUNTIME_CAPTURE_01.md/.json`.
- `T-FO06AA respawn writer source audit`: read-only аудит подтвердил `PlayerRespawnTracker.TeleportToClientRpc` как direct `transform.position` writer, совпадающий с runtime target `y=2502.77`; fall detection server-side использует `y <= _deathY` и delay, а legacy position correction фактически отключена. Точный порядок между respawn `Update`, ClientRpc, owner `ProcessMovement`, `FixedUpdate` и `CharacterController.Move` не задан transaction/fence и остаётся `INCONCLUSIVE`; единственный writer не доказан. Следующий этап — narrow runtime instrumentation без изменения movement semantics. Отчёт: `06AA_RESPAWN_WRITER_SOURCE_AUDIT.md/.json`.
- `T-FO06AA respawn writer runtime instrumentation`: добавлены read-only markers вокруг `PlayerRespawnTracker.Update/PerformRespawn/TeleportToClientRpc`, `NetworkPlayer.Update`, `ProcessMovement` entry, ship-exit и legacy teleport direct position writes. Compile PASS (`No compile errors`); Play Mode не запускался. Следующий capture должен установить frame/sequence order между respawn writer, `CharacterController.Move`, `FixedUpdate`, NGO ticks и baseline markers. Отчёт: `06AA_RESPAWN_WRITER_RUNTIME_INSTRUMENTATION.md/.json`.
- `T-FO06AA runtime capture 02`: instrumentation подтвердила на `frame=181` порядок `FixedUpdate/NGO tick → NetworkPlayer.Update/CharacterController.Move до y=-8.02 → respawn.Update после 0.526s ниже death threshold → PlayerRespawnTracker.TeleportToClientRpc на y=2502.77`; на `frame=182` movement уже начинается с y=2502.77. Историческая initial placement anomaly объяснена server-authoritative fall respawn, а competing writer для перехода не требуется. Ошибок нет, NavMesh warnings сохраняются. Отчёт: `06AA_RUNTIME_CAPTURE_02.md/.json`.
- `T-FO06AA closure`: diagnosis-driven slice закрыт. Initial spawn gate остаётся `PARTIAL_RUNTIME_CONFIRMATION`; историческая placement anomaly классифицирована как `EXPLAINED_AS_SERVER_AUTHORITATIVE_FALL_RESPAWN`; controlled rebase, rollback и runtime rebase readiness не реализованы. Dormant read-only instrumentation сохранена, повторять respawn capture не требуется. Отчёт: `06AA_CLOSURE.md/.json`.
- `T-FO06AB explicit user-controlled runtime driver boundary`: создан fail-closed `GlobalMotionRebaseRuntimeDriver`, который принимает только explicit `UserControlled` request, валидирует transaction/frame/reason, подключается к `GlobalMotionRebaseCoordinator.TryPrepare`, пишет ordered `rebase.*` markers и останавливается с `native_apply_pipeline_not_connected` до concrete native adapters. Automatic trigger, Unity state mutation, rollback и runtime readiness не подключены. Compile PASS (`No compile errors`). Отчёт: `06AB_USER_CONTROLLED_RUNTIME_DRIVER.md/.json`.
- `T-FO06AC native adapter contract`: создан runtime-independent sealed contract для native adapter coverage `Transform`, `Rigidbody`, `ShipDeckNav`, `CameraHistory`, `NetworkBaseline`. Валидация требует explicit IDs, no overlap, current/native-ready adapters и полный набор Capture/Apply/Rebuild/Validate/Restore capabilities. Реальные instances, Unity state mutation, rollback, participant admission и live manifest не подключены. Compile PASS (`No compile errors`). Отчёт: `06AC_NATIVE_ADAPTER_CONTRACT.md/.json`.
- `T-FO06AD runtime readiness gate`: создан fail-closed gate для соединения native adapter set с runtime driver. Gate требует live manifest publication, required manifest coverage, sealed/current/native-ready adapters с полным capability set и admission evidence для каждого participant. Текущие gates остаются `participantAdmission=NOT_READY`, `liveManifestPublication=false`, `runtimeRebaseReadiness=NOT_READY`; Unity state и runtime не изменялись. Compile PASS (`No compile errors`). Отчёт: `06AD_RUNTIME_READINESS_GATE.md/.json`.
- `T-FO06AE live manifest receipt source`: создан pure evidence source для будущей participant admission/live publication. `GlobalMotionRebaseLiveManifestReceiptSource` выдаёт receipt только после required manifest coverage и admission policy pass для каждого entry; validation повторяет digest/count/publisher checks. Runtime discovery, transport publication, native adapters, runtime driver, mutation и rollback не подключены. Текущие gates остаются `participantAdmission=NOT_READY`, `liveManifestPublication=false`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS (`No compile errors`). Отчёт: `06AE_LIVE_MANIFEST_RECEIPT_SOURCE.md/.json`.
- `T-FO06AF live manifest session evidence`: создан fail-closed validator для отличия pure receipt eligibility от фактического наблюдения manifest в конкретной runtime session. Проверяются session identity, publisher observation, server acceptance, peer digest/count match, отсутствие drift и положительный peer count. User-controlled runtime evidence не предоставлено; `liveManifestPublication=NOT_PROVEN`, `participantAdmission=NOT_READY`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS (`No compile errors`). Отчёт: `06AF_LIVE_MANIFEST_SESSION_EVIDENCE.md/.json`.
- `T-FO06AG aggregated readiness bundle`: безопасно объединены уже существующие pure validators в один read-only `GlobalMotionRebaseReadinessBundle`. Фиксированный порядок: manifest/session evidence → native adapter/runtime readiness → final bundle. Новых runtime side effects нет; `manifestSessionProven=false`, `nativeAdaptersReady=false`, `runtimeReady=false`, `liveManifestPublication=NOT_PROVEN`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS (`No compile errors`). Отчёт: `06AG_READINESS_BUNDLE.md/.json`.
- `T-FO06AH runtime driver readiness authorization`: `GlobalMotionRebaseRuntimeDriver` интегрирован с readiness bundle через explicit `TryAuthorizeReadiness`; user-controlled request теперь невозможен без `bundle.IsReady=true`, authorization только в `Idle`, reset очищает authorization. Concrete adapters, valid bundle creation, manifest publication и Unity mutation не подключены; `readinessAuthorization=NOT_GRANTED`, `nativeApplyPipeline=NOT_CONNECTED`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS (`No compile errors`). Отчёт: `06AH_DRIVER_READINESS_AUTHORIZATION.md/.json`.
- `T-FO06AI runtime connection evidence contract`: readiness bundle связан с sealed native adapter set через отдельный pure `GlobalMotionRebaseRuntimeConnectionGate`. Gate выдаёт только immutable connection evidence после проверки готового bundle и полной adapter validation; он не подключает driver, не вызывает adapter methods и не изменяет Unity state. `connectionEvidence=NOT_PROVEN`, `nativeApplyPipeline=NOT_CONNECTED`, `runtimeRebaseReadiness=NOT_READY`. Compile проверяется после импорта нового script; Play Mode не запускался. Отчёт: `06AI_RUNTIME_CONNECTION_EVIDENCE.md/.json`.
- `T-FO06AJ runtime connection authorization gate`: ранее созданный connection evidence token связывается с текущими readiness bundle и sealed adapter set через ordinal digest/session checks и точное совпадение adapter count. Gate выдаёт только immutable authorization evidence; driver, coordinator, native adapter methods и Unity state не затрагиваются. `connectionAuthorization=NOT_GRANTED`, `nativeApplyPipeline=NOT_CONNECTED`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS; Play Mode не запускался. Отчёт: `06AJ_RUNTIME_CONNECTION_AUTHORIZATION.md/.json`.
- `T-FO06AK runtime installation boundary`: добавлена последняя pure boundary перед будущей serial installation. Только valid connection authorization и explicit non-empty user-controlled reason создают immutable installation intent; automatic trigger, driver installation, adapter invocation и Unity mutation исключены. `installationIntent=NOT_CREATED`, `nativeApplyPipeline=NOT_CONNECTED`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS; Play Mode не запускался. Отчёт: `06AK_RUNTIME_INSTALLATION_BOUNDARY.md/.json`.
- `T-FO06AL driver installation authorization`: `GlobalMotionRebaseRuntimeDriver` теперь принимает installation intent только после readiness authorization и exact ordinal match manifest/session identity. `TryRequestUserControlled` требует и readiness, и installation authorization; `TryReset` очищает оба состояния. Driver/coordinator/native pipeline и Unity state не изменяют runtime mutation. `driverInstallationAuthorization=NOT_GRANTED`, `nativeApplyPipeline=NOT_CONNECTED`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS; Play Mode не запускался. Отчёт: `06AL_DRIVER_INSTALLATION_AUTHORIZATION.md/.json`.
- `T-FO06AM driver installation identity evidence`: повторная installation authorization до `TryReset` блокируется, а `InstallationId` добавляется в `rebase.Requested` evidence. Это одноразовая lifecycle-bound identity связь без runtime installation, native adapter invocation или Unity mutation. `driverInstallationAuthorization=NOT_GRANTED`, `nativeApplyPipeline=NOT_CONNECTED`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS; Play Mode не запускался. Отчёт: `06AM_DRIVER_INSTALLATION_IDENTITY_EVIDENCE.md/.json`.
- `T-FO06AN readiness rollover invalidation`: повторная readiness authorization теперь всегда очищает прежний installation intent и требует новой installation authorization; `ReadinessAuthorized` evidence отмечает `installationInvalidated=true`. Stale installation identity не переносится между readiness bundles. Runtime installation, native adapters и Unity mutation не подключены. `driverInstallationAuthorization=NOT_GRANTED`, `nativeApplyPipeline=NOT_CONNECTED`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS; Play Mode не запускался. Отчёт: `06AN_READINESS_ROLLOVER_INVALIDATION.md/.json`.
- `T-FO06AO terminal failure invalidation`: `GlobalMotionRebaseRuntimeDriver.Fail` теперь очищает installation intent и пишет `InstallationInvalidated` перед terminal phase. После `Aborted/Faulted` требуется `TryReset` и новая readiness/installation authorization chain; runtime installation, native adapters и Unity mutation не подключены. `driverInstallationAuthorization=NOT_GRANTED`, `nativeApplyPipeline=NOT_CONNECTED`, `runtimeRebaseReadiness=NOT_READY`. Compile PASS; Play Mode не запускался. Отчёт: `06AO_TERMINAL_FAILURE_INVALIDATION.md/.json`.
- `T-FO06AP consolidated integration status`: проведён сводный review цепочки `T-FO06AA–T-FO06AO`. Pure fail-closed contracts, provenance и invalidation rules подтверждены; дальнейшее добавление эквивалентных micro-contracts остановлено. Не доказаны concrete adapters, live manifest/peer evidence, participant admission, valid readiness bundle, runtime installation, native Apply/Rebuild/Validate/Publish и rollback. Следующий gate — `T-FO06AQ` user-controlled runtime evidence collection and adapter-readiness review. `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06AP_CONSOLIDATED_INTEGRATION_STATUS.md/.json`.
- `T-FO06AQ user-controlled runtime evidence review`: комплексный gate объединяет live manifest session validation, participant admission/native adapter readiness, complete runtime proof, rollback evidence и identity/frame/count matching. Gate не собирает runtime data и не создаёт readiness bundle без фактического evidence package; текущий review заблокирован отсутствием user-controlled runtime evidence. `runtimeRebaseReadiness=NOT_READY`.
- `T-FO06AQ follow-up pointwise Play Mode evidence review`: зафиксирован один Host capture с initial baseline (`frame=885`), стабильным binding, движением, jump, camera/deck/passenger observations и объяснённым `PlayerRespawnTracker` fall-respawn (`frame=901`). Controlled rebase, post-rebase continuity, rollback, peer manifest agreement, participant admission и native adapter installation не наблюдались; `adapter=Ready` относится только к initial-baseline pipeline. Scheduling anomaly отмечена как observation, не как доказательство rebase ordering. Полный лог `Q:\Project-c_logs\01.txt` дополнительно проверен точечно: `[T-FO06Y]` содержит `7995` строк, `rebase/rollback/manifest/admission/Exception/Error` совпадений нет, NavMesh warnings — `247`; диапазон capture `frames=885–8879`. `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06AQ_RUNTIME_CAPTURE_FOLLOWUP.md/.json`.
- `T-FO06BA NetworkBaseline observation-only audit`: полный `[T-FO06Y]` capture сопоставлен с pure `T-FO06AZ` contract. Initial baseline/acknowledgement подтверждены, initial tick/sequence stream наблюдается до `revision=198`, `sequence=5227`, `NetworkTick=5942`, но evidence остаётся `ObservationOnly`: post-rebase continuity, rollback, participant admission и restorable capability не доказаны. `247` NavMesh warnings сохраняют ShipDeckNav blocker; `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06BA_NETWORK_BASELINE_OBSERVATION_AUDIT.md/.json`.
- `T-FO06BB live manifest/admission runtime census`: source audit подтвердил наличие только pure participant admission/manifest receipt/session gates и contract-only adapter set/driver. Concrete runtime publisher/discovery/admission bridge в audited source paths не найден; actor participants не являются live manifest publisher-ами. `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, BootstrapScene installation не авторизована, `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06BB_LIVE_MANIFEST_ADMISSION_RUNTIME_CENSUS.md/.json`.
- `T-FO06BC live manifest runtime bridge`: создан первый dormant runtime code slice `GlobalMotionRebaseLiveManifestRuntimeBridge` с owner-reviewed source interface, server receipt publication и exact peer digest/count acknowledgement. Добавлен pure validator (`6/6` PASS). Component/source binding не установлены, BootstrapScene, native adapters, driver, `FullTransaction` и rollback не изменялись; `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06BC_LIVE_MANIFEST_RUNTIME_BRIDGE.md/.json`. Отчёт: `06AQ_USER_CONTROLLED_EVIDENCE_REVIEW.md/.json`.
- `T-FO06BD runtime manifest source compile gate`: создан `GlobalMotionRebaseSceneRuntimeManifestSource` для детерминированного census реальных `ShipController`, `ShipDeckNav` и `NpcBrain` с фиксированным покрытием `22/20/20`, проверками deck/passenger readiness и делегированием admission evidence отдельному provider. Compile PASS после добавления `using ProjectC.Player;`. Provider, synthetic admission evidence, bridge/source binding, BootstrapScene, native adapters, driver, `FullTransaction` и Play Mode не подключались; live publication и peer agreement не наблюдались; `runtimeRebaseReadiness=NOT_READY`. Отчёт `06BD_RUNTIME_MANIFEST_SOURCE_COMPILE_GATE.md/.json`.
- `T-FO06BE runtime admission provider boundary audit`: в audited source paths concrete provider и runtime producers для complete native adapter readiness, complete runtime proof и rollback evidence не найдены. Immutable proof/rollback envelopes являются только входными evidence, а existing Transform/Rigidbody/Camera adapters не зарегистрированы и не покрывают ShipDeckNav/NetworkBaseline. Synthetic all-true provider намеренно не создавался; BootstrapScene/source binding не выполнялись; поиск вне audited paths остаётся inconclusive. `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Следующий допустимый gate — provenance-bearing evidence source contracts. Отчёт `06BE_RUNTIME_ADMISSION_PROVIDER_BOUNDARY_AUDIT.md/.json`.
- `T-FO06BF provenance-bearing admission provider boundary`: создан dormant fail-closed `GlobalMotionRebaseAdmissionEvidenceProvider` с четырьмя explicit source contracts для reviewed admission, adapter readiness, runtime proof и rollback. Provider не создаёт evidence, требует реальные источники, валидирует adapter/proof/rollback contracts и проверяет participant/frame lineage. Concrete producers, BootstrapScene binding, native registration, live publication и Play Mode не выполнялись; `runtimeRebaseReadiness=NOT_READY`. Отчёт `06BF_PROVENANCE_ADMISSION_PROVIDER_BOUNDARY.md/.json`.
- `T-FO06BG native adapter evidence source`: создан первый concrete provenance producer `GlobalMotionRebaseNativeAdapterEvidenceSource`. Он создаёт explicit Transform/Rigidbody/CameraHistory adapters только из reviewed targets и fail-closed требует ShipDeckNav/NetworkBaseline adapter sources; частичное покрытие не принимается. Source компилируется, но runtime registration, BootstrapScene binding, live publication и Play Mode не выполнялись; `runtimeRebaseReadiness=NOT_READY`. Отчёт `06BG_NATIVE_ADAPTER_EVIDENCE_SOURCE.md/.json`.
- `T-FO06BH ShipDeckNav synchronous lifecycle contract`: создан runtime-independent fail-closed контракт ordered lifecycle `Unbound → RegistrationRequested → Registered → Ready → SnapshotCaptured → RebuildRequested → Rebuilt → Validated → Restored` с generation lineage и обязательными synchronous rebuild/restore capability. Concrete ShipDeckNav adapter и runtime registration не создавались; synchronous lifecycle остаётся UNVERIFIED, `runtimeRebaseReadiness=NOT_READY`. Отчёт `06BH_SHIP_DECK_NAV_SYNCHRONOUS_LIFECYCLE_CONTRACT.md/.json`.
- `T-FO06BI NetworkBaseline adapter boundary`: создан runtime-independent protocol-owned boundary `GlobalMotionRebaseNetworkBaselineAdapterBoundary`. Observation-only evidence отклоняется; для будущего adapter требуются Restorable baseline, capture/restore, ownership-restore и lifetime-restore boundaries. Concrete producer/adapter, NGO mutation, BootstrapScene binding и Play Mode не выполнялись; `runtimeRebaseReadiness=NOT_READY`. Отчёт `06BI_NETWORK_BASELINE_ADAPTER_BOUNDARY.md/.json`.
- `T-FO06BJ runtime proof/evidence producer blocker review`: source audit подтвердил, что `GlobalMotionRuntimeEvidenceProbe` остаётся log-only и не создаёт structured runtime proof или rollback evidence. Автоматический parser log → admission evidence намеренно не создавался; user-controlled structured intake отсутствует. Search outside audited paths остаётся inconclusive, `runtimeRebaseReadiness=NOT_READY`. Отчёт `06BJ_RUNTIME_PROOF_AND_EVIDENCE_PRODUCER_BLOCKER_REVIEW.md/.json`.
- `T-FO06BK user-controlled structured evidence intake`: создан concrete `GlobalMotionRebaseUserControlledEvidenceIntake`, принимающий только внешние validated runtime-proof/rollback envelopes с explicit capture reference и `userSupplied=true`. До submit оба источника fail closed; automatic log parsing, provider binding, BootstrapScene и Play Mode не выполнялись. `runtimeRebaseReadiness=NOT_READY`. Отчёт `06BK_USER_CONTROLLED_STRUCTURED_EVIDENCE_INTAKE.md/.json`.
- `T-FO06BL provider source binding API`: `GlobalMotionRebaseAdmissionEvidenceProvider` получил explicit `TryConfigureSources`/`TryValidateSourceBindings`, проверяющие четыре typed source contracts до admission chain. API реализован, но serial binding не выполнялся: reviewed source, полный adapter set и rollback producer отсутствуют; BootstrapScene и Play Mode не изменялись, `runtimeRebaseReadiness=NOT_READY`. Отчёт `06BL_PROVIDER_SOURCE_BINDING_API.md/.json`.
- `T-FO06BM reviewed admission evidence intake`: создан concrete `GlobalMotionRebaseReviewedAdmissionEvidenceIntake` для explicit owner review identity/policy/catalog. Runtime adapter/proof/rollback flags intake принудительно оставляет `false`; review submission и provider binding не выполнялись, `runtimeRebaseReadiness=NOT_READY`. Отчёт `06BM_REVIEWED_ADMISSION_EVIDENCE_INTAKE.md/.json`.
- `T-FO06AR user runtime evidence package`: добавлена pure intake boundary `GlobalMotionRebaseUserControlledEvidencePackageGate`, которая запечатывает только ready review с user-supplied capture reference. Runtime capture не выполняется автоматически. Для следующего gate пользователю нужен один полный Host/Client capture из canonical BootstrapScene с baseline, manifest peer agreement, movement/jump, camera/history, ShipDeckNav/passenger, NGO/physics ordering и rollback evidence; controlled rebase phases остаются `UNVERIFIED`, пока driver/adapters не установлены. `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06AR_USER_RUNTIME_CAPTURE_PROTOCOL.md/.json`.
- `T-FO06AT runtime transaction execution boundary`: driver теперь имеет ordered adapter execution path `TryApply → TryRebuild → TryValidate → TryPublish`, переводит transaction через `Applied → PhysicsSynchronized → Validated → Published → Completed` и использует explicit `TryRestore` вместе с coordinator abort для `RollbackBegun → Restored → RollbackCompleted`. При rollback installation intent инвалидируется. Concrete adapters, runtime driver installation, manifest publication, Unity mutation и Play Mode не подключены; `runtimeRebaseReadiness=NOT_READY`. Compile/validation PASS. Отчёт: `06AT_RUNTIME_TRANSACTION_EXECUTION_BOUNDARY.md`.
- `T-FO06AU concrete adapter readiness audit`: source census обнаружил `0` concrete `IGlobalMotionRebaseRuntimeDriverAdapter` и `0` concrete `IGlobalMotionNativeAdapter`; component search для `GlobalMotionRebaseRuntimeDriver` в BootstrapScene вернул `0` объектов. Existing `GlobalMotionPoseAdapter` не является rebase adapter и не покрывает rollback/full coverage. Из-за отсутствия полного runtime evidence package, participant admission, manifest publication и rollback proof переход к full concrete adapter set и scene installation запрещён. `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06AU_CONCRETE_ADAPTER_READINESS_AUDIT.md`.
- `T-FO06AV Transform native adapter`: создан explicit `GlobalMotionTransformNativeAdapter` для одного owner-reviewed Transform root с transaction-scoped snapshot/apply/validate/restore semantics. Adapter не регистрируется автоматически, не устанавливается в BootstrapScene и покрывает только `Transform`; остальные четыре native domains, participant admission, manifest publication и runtime rollback evidence остаются незакрытыми. Compile/validation PASS; `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06AV_TRANSFORM_NATIVE_ADAPTER.md`.
- `T-FO06AW Rigidbody native adapter`: создан explicit `GlobalMotionRigidbodyNativeAdapter` для одного owner-reviewed Rigidbody с transaction-scoped snapshot позиции/rotation/velocity, Apply, Physics.SyncTransforms boundary, Validate и Restore. Adapter не регистрируется автоматически и не меняет BootstrapScene; ShipDeckNav, CameraHistory, NetworkBaseline, manifest/admission и runtime evidence остаются незакрытыми. Compile/validation PASS; `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06AW_RIGIDBODY_NATIVE_ADAPTER.md`.
- `T-FO06AX CameraHistory native adapter`: created explicit `GlobalMotionCameraHistoryNativeAdapter` for one owner-reviewed `SpringArmCamera` with transaction-scoped camera pose, lag/collision history and identity snapshot, plus Apply/Validate/Restore through dormant `SpringArmCamera` APIs. The adapter is not auto-registered and does not change BootstrapScene; live camera ownership/history evidence, ShipDeckNav, NetworkBaseline, manifest/admission and runtime rollback remain open. Compile PASS; `runtimeRebaseReadiness=NOT_READY`.
- `T-FO06AY remaining adapter blocker review`: source review завершил проверку продолжения concrete adapter integration. `ShipDeckNav` заблокирован asynchronous NavMesh registration/passenger readiness и отсутствием synchronous rollback seam; `NetworkBaseline` заблокирован отсутствием отдельного readiness/rollback capability contract и protocol-owned NGO lifecycle. Concrete adapters не создаются до отдельных reviewed design gates; `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06AY_REMAINING_ADAPTER_BLOCKERS.md`.
- `T-FO06AZ NetworkBaseline readiness/continuity contract`: создан pure fail-closed contract для `NetworkObject` identity, ownership/spawn lifetime generations, baseline tick/sequence continuity, acknowledgement lineage и явного различия `ObservationOnly`/`Restorable`. Observation-only evidence не может заявлять restore capability; restorable classification требует отдельного полного evidence. Concrete adapter, изменение `FullTransaction`, registration в `GlobalMotionNativeAdapterSet`, runtime driver, BootstrapScene, manifest/admission и Play Mode не подключались. `19/19` pure checks PASS, compile PASS; `NetworkBaseline adapter=BLOCKED`, `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06AZ_NETWORK_BASELINE_READINESS_CONTINUITY_CONTRACT.md/.json`.
- `T-FO06BN protocol-owned NetworkBaseline reversible capability`: создан pure fail-closed capability boundary для будущего server-coordinated NGO baseline transaction. Требуются Restorable evidence, server authority, protocol ownership, capture/apply/validate/restore boundaries, ownership/lifetime/baseline-generation restore и transaction identity; receipt проверяет ordered phase lineage. `16/16` pure checks PASS, compile PASS. Concrete NetworkBaseline producer/adapter, ShipDeckNav adapter, provider binding, `GlobalMotionNativeAdapterSet`, runtime driver, BootstrapScene, rollback evidence и Play Mode не подключались; `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06BN_NETWORK_BASELINE_REVERSIBLE_CAPABILITY.md/.json`.
- `T-FO06BO protocol-owned NetworkBaseline native adapter seam`: создан explicit `IGlobalMotionNetworkBaselineTransactionHost` и dormant `GlobalMotionNetworkBaselineNativeAdapter`. Adapter делегирует NGO-owned capture/apply/rebuild/validate/restore только protocol-owned host-у, сообщает `NativeReady` лишь при полном T-FO06BN capability и проверяет participant/rollback identity. `6/6` pure checks PASS, compile PASS. Реального host, registration, provider binding, `GlobalMotionNativeAdapterSet`, runtime driver, BootstrapScene, rollback evidence и Play Mode нет; `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06BO_NETWORK_BASELINE_NATIVE_ADAPTER_SEAM.md/.json`.
- `T-FO06BP protocol-owned NetworkBaseline host readiness audit`: source-level review `GlobalMotionReplicator` подтвердил observation/controlled-reactivation seams (`Control`, accepted baseline read, activation, acknowledgement, stop и lifecycle guards), но не reversible protocol host. Отсутствуют transaction-safe capture/restore ownership, spawn/lifetime generation, control/binding/ack lineage и server-owned rollback receipts; private state не образует внешний host contract. Concrete host не создавался, adapter остаётся `SEAM_ONLY`, provider binding/registration/BootstrapScene/Play Mode не выполнялись, `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06BP_NETWORK_BASELINE_HOST_READINESS_AUDIT.md/.json`.
- `T-FO06BQ server-owned NetworkBaseline protocol ledger boundary`: создан pure receipt ledger для reviewed NGO protocol change. Ledger связывает transaction/participant/lifetime/generation identity с monotonic ordered phases `CaptureRequested → ... → Completed` и terminal `Faulted`; incomplete restore или phase skip отклоняются. Validator: `10/10` pure checks PASS, compile PASS. Контракт не реализует NGO host и не меняет runtime; `GlobalMotionNetworkBaselineNativeAdapter=SEAM ONLY`, `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06BQ_NETWORK_BASELINE_PROTOCOL_LEDGER.md/.json`.
- `T-FO06BR NGO lifecycle receipt source audit`: source audit по `GlobalMotionReplicator`, `NetworkPlayer`, `NpcSpawner` и `PlayerRespawnTracker` подтвердил раздельные spawn/despawn/respawn seams, но не выявил единого server-owned producer-а для exact spawn/lifetime/ownership generations, control/binding/ack lineage и reversible partial-transaction restore. Synthetic host из callbacks не создавался; полный census за пределами audited paths остаётся inconclusive. `NetworkBaseline adapter=SEAM ONLY`, `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06BR_NGO_LIFECYCLE_RECEIPT_SOURCE_AUDIT.md/.json`.
- `T-FO06BS NGO ownership/lifetime producer census`: расширенный поиск по `Assets/_Project/Scripts` не нашёл прямых `ChangeOwnership`/`RemoveOwnership` вызовов и выявил 11 независимых spawn/despawn producer paths. Unified server-owned lifecycle owner отсутствует; synthetic ownership host и despawn/recreate rollback не создавались. Package internals, generated code, serialized scene callbacks и external tooling остаются inconclusive; `NetworkBaseline adapter=SEAM ONLY`, `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Отчёт: `06BS_NGO_OWNERSHIP_LIFETIME_PRODUCER_CENSUS.md/.json`.
- `T-FO06BT concrete NetworkBaseline protocol host`: created `GlobalMotionNetworkBaselineProtocolHost` as a concrete `MonoBehaviour` bound to `GlobalMotionReplicator` through `RequireComponent` and lazy resolution. It reads the accepted server baseline and builds observation-only evidence/capture envelopes with transaction/lifetime identity tracking. `Apply/Rebuild/Validate/Restore` remain fail-closed; `IsRestorable=false` and no `Restorable` capability is declared. Validator: `8/8` pure checks PASS; compile PASS. Runtime registration, provider binding, native adapter set, BootstrapScene and Play Mode were not executed; `NativeReady=false`, `runtimeRebaseReadiness=NOT_READY`. Report: `06BT_NETWORK_BASELINE_PROTOCOL_HOST_IMPLEMENTATION.md/.json`.
- `T-FO06BU NetworkBaseline native adapter source binding`: created `GlobalMotionNetworkBaselineNativeAdapterSource` as a concrete `MonoBehaviour` implementing `IGlobalMotionNativeAdapter` and delegating to the protocol host through the existing adapter seam. It declares only `NetworkBaseline` coverage, remains `NativeReady=false` until the host provides reviewed `Restorable` capability, and does not self-register or bind the provider. Validator: `6/6` pure checks PASS; compile PASS; `git diff --check=PASS`. `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Report: `06BU_NETWORK_BASELINE_NATIVE_ADAPTER_SOURCE.md/.json`.
- `T-FO06BV native adapter evidence source binding`: added explicit `TryConfigureSources` and `TryValidateSourceBindings` APIs to `GlobalMotionRebaseNativeAdapterEvidenceSource`. The source validates reviewed Transform/Rigidbody/Camera targets, camera owner identity and typed ShipDeckNav/NetworkBaseline adapter sources before constructing a candidate set; incomplete binding remains rejected. Validator: `6/6` pure checks PASS; compile PASS; `git diff --check=PASS`. No adapter set registration, provider binding, BootstrapScene mutation or Play Mode; `runtimeRebaseReadiness=NOT_READY`. Report: `06BV_NATIVE_ADAPTER_EVIDENCE_SOURCE_BINDING.md/.json`.
- `T-FO06BW admission source handoff preflight`: pure Edit Mode graph validation confirmed typed handoff from `GlobalMotionRebaseNativeAdapterEvidenceSource` to `GlobalMotionRebaseAdmissionEvidenceProvider`. Bindings validate, but admission remains blocked by absent submitted reviewed evidence and non-ready NetworkBaseline adapter; typed stubs are used only to test contracts. Validator: `6/6` pure checks PASS; compile PASS; `git diff --check=PASS`. No adapter registration, live publication, provider runtime binding, BootstrapScene mutation or Play Mode; `runtimeRebaseReadiness=NOT_READY`. Report: `06BW_ADMISSION_SOURCE_HANDOFF.md/.json`.
- `T-FO06BX ShipDeckNav native adapter source`: added a concrete observation-only `GlobalMotionShipDeckNavProtocolHost`, typed `IGlobalMotionShipDeckNavTransactionHost`, `GlobalMotionShipDeckNavNativeAdapter` and `GlobalMotionShipDeckNavNativeAdapterSource`. The host reads actual registration/instance/readiness flags but leaves passenger attachment generation, synchronous rebuild and restore unavailable; the adapter therefore remains `NativeReady=false`. Validator: `6/6` pure checks PASS; `check_compile_errors=No compile errors`; `git diff --check=PASS`. No adapter registration, provider binding, live publication, BootstrapScene mutation or Play Mode; `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Report: `06BX_SHIP_DECK_NAV_NATIVE_ADAPTER_SOURCE.md/.json`.
- `T-FO06BY ShipDeckNav passenger provenance binding`: extended `GlobalMotionShipDeckNavProtocolHost` with explicit owner-reviewed `NpcBrain[]` binding. The host validates attachment active state, deck navigation, proxy creation, proxy `isOnNavMesh` and deck identity without automatic discovery or mutation. Empty/unready passenger binding remains rejected; synchronous rebuild/restore remain unavailable. Validator: `7/7` pure checks PASS; `check_compile_errors=No compile errors`; `git diff --check=PASS`. No adapter registration, provider binding, BootstrapScene mutation or Play Mode; `runtimeRebaseReadiness=NOT_READY`. Report: `06BY_SHIP_DECK_NAV_PASSENGER_PROVENANCE.md/.json`.
- `T-FO06BZ ShipDeckNav native snapshot boundary`: added transaction-scoped `ShipDeckNavFloatingOriginSnapshot`, registration generation and explicit `TryCaptureFloatingOriginSnapshot`/`TryRebuildFloatingOriginSnapshot`/`TryRestoreFloatingOriginSnapshot` APIs. `RegisterAt` supports direct synchronous NavMesh re-registration only when explicitly called by a future transaction host; existing async queue remains unchanged. Passenger/proxy state and adapter host invocation remain unimplemented, so `NativeReady=false`, `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED` and `runtimeRebaseReadiness=NOT_READY`. Validator: `8/8` pure checks PASS; `check_compile_errors=No compile errors`; `git diff --check=PASS`. Report: `06BZ_SHIP_DECK_NAV_NATIVE_SNAPSHOT_BOUNDARY.md/.json`.
- `T-FO06CA NPC ship-deck snapshot contract`: added `GlobalMotionNpcShipDeckSnapshot` plus explicit server-owned capture/restore methods on `NpcBrain` for ship attachment, parent/local/world pose, agent flags and deck proxy state. The APIs are not auto-called and are not connected to the adapter host; combined NavMesh/passenger transaction and rollback remain open. Validator: `9/9` pure checks PASS; `check_compile_errors=No compile errors`; `git diff --check=PASS`. `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Report: `06CA_NPC_SHIP_DECK_SNAPSHOT_CONTRACT.md/.json`.
- `T-FO06CB combined ShipDeckNav and passenger transaction host`: создан dormant `GlobalMotionShipDeckCombinedTransactionHost` с explicit reviewed `NpcBrain[]` binding и ordered capture/rebuild/validate/restore API. Исправлен compile error `CS0177`, pure validator `6/6` PASS; host не реализует adapter, не регистрируется и не открывает readiness. Partial passenger rollback и attachment/lifetime generation lineage остаются отдельными gates; `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`.
- `T-FO06CC combined ShipDeckNav rollback result and binding lineage`: добавлены `GlobalMotionShipDeckCombinedTransactionContract`/`Result`, frame и reviewed passenger binding generations, snapshot identity checks и reverse-order best-effort rollback accounting. Pure validator `7/7` PASS; host по-прежнему не является adapter и не подключён к runtime. NGO attachment/lifetime ledger и atomic Unity rollback остаются недоказанными, `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`.
- `T-FO06CD passenger attachment and lifetime generation source gate`: source audit подтвердил explicit attachment/`NetworkObjectId`/`IsSpawned`, но не нашёл server-owned monotonic ship lifetime generation producer или protocol-owned attachment generation ledger в проверенных путях. Добавлены `GlobalMotionShipDeckPassengerGeneration`, `IGlobalMotionShipDeckPassengerGenerationSource` и fail-closed contract; `5/5` pure checks PASS. Concrete source отсутствует, search за пределами audited paths inconclusive, `runtimeRebaseReadiness=NOT_READY`.
- `T-FO06CE ShipDeck passenger protocol ledger boundary`: добавлены immutable passenger ledger receipts и pure contract с transaction/frame/generation identity, ordered capture/rebuild/validate/restore lineage, ранним rollback path и terminal `Faulted`. После fail-closed исправлений validator дал `8/8` pure checks PASS; compile и script validation PASS. Ledger не подключён к `NpcBrain`, `ShipDeckNav`, `GlobalMotionNativeAdapterSet` или runtime driver; concrete producer остаётся unbound, `runtimeRebaseReadiness=NOT_READY`. Report: `06CE_SHIP_DECK_PASSENGER_PROTOCOL_LEDGER.md/.json`.
- `T-FO06CF NpcBrain passenger generation source audit`: подтверждены server-only explicit attach/detach, ship spawn guard, parent attachment, `ShipDeckNav.IsReady` и snapshot readiness, но не найдены реализация generation source, monotonic ship/attachment generations или server-owned lifecycle receipts. `NpcBrain` нельзя адаптировать без synthetic generation из `IsSpawned`/object reference; integration остаётся BLOCKED. Search за пределами audited paths inconclusive, `runtimeRebaseReadiness=NOT_READY`. Report: `06CF_NPC_PASSENGER_GENERATION_SOURCE_AUDIT.md/.json`.
- `T-FO06CG ShipDeck passenger lifecycle producer contract`: создан protocol-owned receipt contract с `ShipRegistered`, `PassengerAttached`, `PassengerDetached` и terminal `ShipInvalidated`, explicit ship/attachment generations и server/protocol ownership. Pure validator `8/8` PASS, compile и script validation PASS. Реальный producer и binding к `NpcBrain`/`ShipDeckNav` не выполнены; `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Report: `06CG_SHIP_DECK_PASSENGER_LIFECYCLE_PRODUCER_CONTRACT.md/.json`.
- `T-FO06CH ShipDeck passenger lifecycle source binding preflight`: создан pure preflight для explicit active `PassengerAttached` receipts. Он проверяет единую ship/lifetime identity, уникальность passenger IDs, binding generation и ownership; не выполняет discovery, source binding или runtime publication. Validator `8/8` PASS, compile и script validation PASS; `GlobalMotionNativeAdapterSet=EMPTY / UNSEALED`, `runtimeRebaseReadiness=NOT_READY`. Report: `06CH_SHIP_DECK_PASSENGER_LIFECYCLE_SOURCE_BINDING.md/.json`.
- `T-FO06CI ShipDeck passenger lifecycle binding handoff`: добавлен explicit handoff contract и dormant API в `GlobalMotionShipDeckCombinedTransactionHost`. Handoff проверяет reviewed passenger count, binding generation, ship lifetime identity и ownership, но не создаёт receipts и не запускает capture/rebuild/restore. Validator `8/8` PASS, compile и script validation PASS; runtime producer/source binding отсутствуют, `runtimeRebaseReadiness=NOT_READY`. Report: `06CI_SHIP_DECK_PASSENGER_LIFECYCLE_BINDING_HANDOFF.md/.json`.
- `T-FO06CJ combined ShipDeck host lifecycle binding preflight`: Edit Mode validator подтвердил фактический handoff двух reviewed `NpcBrain` sources и explicit `PassengerAttached` receipts через host API, включая rejection mismatched binding generation. Validator `9/9` PASS; compile и script validation PASS. Runtime producer, source binding, adapter registration и Play Mode не выполнялись; `runtimeRebaseReadiness=NOT_READY`. Report: `06CJ_COMBINED_HOST_LIFECYCLE_BINDING_PREFLIGHT.md/.json`.
- `T-FO06CK combined snapshot lifecycle binding identity gate`: `GlobalMotionShipDeckCombinedSnapshot` теперь явно переносит полный `GlobalMotionShipDeckPassengerLifecycleBindingReceipt`; `TryCapture` требует reviewed lifecycle binding, а snapshot validation повторно проверяет host handoff и identity equality. Pure validator `10/10` PASS; `check_compile_errors=No compile errors`; runtime producer, adapter registration и Play Mode не выполнялись; `runtimeRebaseReadiness=NOT_READY`. Report: `06CK_COMBINED_SNAPSHOT_LIFECYCLE_BINDING_IDENTITY.md/.json`.
- `T-FO06CL ShipDeck lifecycle owner audit`: read-only audit подтвердил server-side attachment seams `NpcBrain`, реальный server-owned crew spawn producer `ShipCrewSpawner` и ship lifecycle registration `NpcShipController`, но не нашёл legitimate `IGlobalMotionShipDeckPassengerGenerationSource` implementation, monotonic generation ledger или runtime binding. Synthetic producer и inference из `IsSpawned` не создавались; provider/adapter binding не выполнялись, `BootstrapScene` unchanged, Play Mode not run. Вердикт: **INCONCLUSIVE / INTEGRATION BLOCKED**, `runtimeRebaseReadiness=NOT_READY`. Report: `06CL_SHIP_DECK_LIFECYCLE_OWNER_AUDIT.md/.json`.
- `T-FO06CM protocol-owned ShipDeck lifecycle coordinator design gate`: broadened read-only census подтвердил раздельные seams `NpcShipController` ship registration/lifetime, `ShipCrewSpawner` spawn/despawn, `NpcBrain` attach/detach, `ShipDeckNav` readiness и NGO spawn/despawn/ownership boundaries. Design decision выбирает `GlobalMotionShipDeckPassengerLifecycleCoordinator` как единственного protocol-owned server coordinator/ledger owner; `NpcBrain` и `ShipCrewSpawner` намеренно не выбраны по одному лишь наличию seams. Зафиксированы receipt mapping `ShipRegistered`, `PassengerAttached`, `PassengerDetached`, terminal `ShipInvalidated`, monotonic ship/passenger generations, identity/ownership/order/invalidation rules и future path через `IGlobalMotionShipDeckPassengerGenerationSource` к combined transaction host. Implementation и все bindings не выполнялись; synthetic generation из `IsSpawned`, `NetworkObjectId`, object references и host-local binding generation запрещена, `BootstrapScene` unchanged, Play Mode not run, `runtimeRebaseReadiness=NOT_READY`. Search outside audited paths remains inconclusive. Report: `06CM_SHIP_DECK_LIFECYCLE_COORDINATOR_DESIGN_GATE.md/.json`.
- `T-FO06CN protocol-owned ShipDeck lifecycle coordinator implementation gate`: создан dormant runtime-independent `GlobalMotionShipDeckPassengerLifecycleCoordinator`, реализующий `IGlobalMotionShipDeckPassengerGenerationSource`. Coordinator принимает только explicit server-authorized facts для `ShipRegistered`, `PassengerAttached`, `PassengerDetached` и terminal `ShipInvalidated`, сам выдаёт monotonic never-reused ship lifetime/attachment generations и strict ledger ordinals, отклоняет stale/duplicate/mismatched/out-of-order/post-invalidation facts и fail-closed выдаёт active source generation. Добавлен pure Edit Mode validator: `11/11` PASS; `check_compile_errors=No compile errors`; script validation `0 warnings / 0 errors`. Runtime bindings к `NpcShipController`, `ShipCrewSpawner`, `NpcBrain`, `ShipDeckNav`, combined host, provider, adapter set, `BootstrapScene` и runtime driver не выполнялись; `BootstrapScene` unchanged, Play Mode not run, `runtimeRebaseReadiness=NOT_READY`. Report: `06CN_SHIP_DECK_LIFECYCLE_COORDINATOR_IMPLEMENTATION.md/.json`.
- `T-FO06CO reviewed coordinator source-binding/fact-handoff gate`: добавлен typed lossless mapping из `GlobalMotionShipDeckPassengerLifecycleCoordinatorReceipt` в downstream `GlobalMotionShipDeckPassengerLifecycleReceipt`. Mapping сохраняет stable protocol `ShipId`, supplemental `ShipNetworkObjectId`, coordinator-owned ship lifetime, passenger attachment generation, deck identity, contiguous coordinator ledger ordinal, server/protocol ownership, lifecycle event kind и terminal invalidation reason; sequence validation отклоняет incomplete, stale/duplicate/out-of-order/post-invalidation provenance и не выводит identity/generations из `IsSpawned`, object references, `ShipDeckNav.RegistrationGeneration`, callback order или host-local binding generation. Pure Edit Mode validator: `7/7` PASS; `check_compile_errors=No compile errors`; `validate_script=0 warnings / 0 errors`. Existing T-FO06CN validator: `11/11` PASS. Runtime caller binding, provider/adapter registration, `BootstrapScene`, scenes/prefabs and Play Mode remain untouched; `runtimeRebaseReadiness=NOT_READY`. Report: `06CO_SHIP_DECK_LIFECYCLE_COORDINATOR_SOURCE_BINDING_HANDOFF.md/.json`.
- `T-FO06CP explicit fact-source binding readiness audit`: docs-only read-only audit зафиксировал exact seams `NpcShipController` (server registration/despawn), `ShipCrewSpawner` (server crew spawn/despawn), `NpcBrain` (explicit server attach/detach), `ShipDeckNav` (async registration/readiness) и `GlobalMotionShipDeckCombinedTransactionHost` (reviewed passenger binding и ordered transaction API). Four-phase ingress matrix для `ShipRegistered`, `PassengerAttached`, `PassengerDetached`, `ShipInvalidated` требует stable protocol `ShipId`/passenger identity, explicit ship lifetime/attachment generations, server/protocol ownership, contiguous ordering и terminal invalidation. Запрещены inference из `IsSpawned`, `NetworkObjectId` как stable identity, Unity/NGO object references, `ShipDeckNav.RegistrationGeneration`, callback order и host-local binding generation. T-FO06CO `7/7` и T-FO06CN `11/11` отмечены только как inherited pure validation evidence, не runtime evidence. Runtime caller binding, provider/adapter/`BootstrapScene` и Play Mode не выполнялись; outside audited paths `INCONCLUSIVE`, `runtimeRebaseReadiness=NOT_READY`. Report: `06CP_SHIP_DECK_LIFECYCLE_COORDINATOR_FACT_SOURCE_BINDING_READINESS_AUDIT.md/.json`.

Пакетирование допустимо только для независимых runtime-independent contracts и validators, как в T-FO06W, T-FO06AZ и T-FO06BN. ShipDeckNav synchronous lifecycle, runtime camera ownership, passenger attachment completion, NGO/physics ordering, final manifest review, scene-owned gameplay classification и native rollback должны оставаться отдельными serial/user-controlled gates.

Runtime rebase readiness остаётся **NOT READY**; `GroundPlane_0_0`, `FloatingOriginMP`, player-only shifting и generic shared `SetParent` остаются исключёнными.

## 4. Приёмка полного решения

- Позиция прежнего FAIL (~56.5 км), 30 км+, origin: обычные Idle/walk/run/jump, без замороженного Animator и без hide/render tricks; пользовательское видео/скриншоты.
- Host и минимум два клиента: рядом, далеко друг от друга, повторно вместе; каждый локальный игрок остаётся в принятом точностном бюджете.
- Принудительный и пороговый rebase при движении/прыжке/бое/пилотировании, на палубе и в момент посадки/выхода.
- Корабль с пассажирами/именованным экипажем, rotation, docking/undocking, recall, autopilot/cruise, NPC торговля, navmesh readiness/handoff.
- Телепорт/respawn/save/load/reconnect/late join, scene load/unload, pool reuse, смена authority/parent, пакеты до и после shift.
- Pickup/chest/resources, Q001 dialogue/quest zones, combat projectiles/AOE/animation events, equipment/body swap и modal input gate.
- Глобальная высота/коридоры, штормы/ветер/облака/lighting, карта/компас/индикаторы, world-space shaders/particles/trails/lines.
- Измерения CPU/GC/rebase spikes, трафика и расстояний физики от origin. Не обещать абсолютное отсутствие любого джиттера по одному compile PASS.

## 5. Работа с результатами

См. отдельные отчёты этапов в этом каталоге. Не переписывать отрицательную историю T-JITTER18 и не выдавать ненайденные/непрочитанные данные за доказательства. Неизвестные active components, mesh-local bounds, shader semantics и runtime порядок явно маркируются UNVERIFIED до прямой проверки.
