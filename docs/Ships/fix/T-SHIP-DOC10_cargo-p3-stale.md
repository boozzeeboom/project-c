# T-SHIP-DOC10 — перепроверка: Cargo P3 stale-доки (существуют ли?)

> Тикет: `T-SHIP-DOC10`. Дата: 2026-09-18. Статус: **ПОДТВЕРЖДЕНО ЧАСТИЧНО (файлы уже в архиве)**.
> Scope: только перепроверка + пометка в плане. Кода нет.

## Что проверяли

Утверждение ревью (§4 несостыковок): `roadmap-integration:200`,
`analysis-composite-ship:30`, `00_COMPOSITE_SUMMARY:67` врут
(«CargoSystem отсутствует/не делаем»); чинит `SHIP_REFACTOR P3`.

## Факт на текущем main (сверено)

- Трёх файлов **нет** в `docs/Ships/`: `00_COMPOSITE_SHIP_SUMMARY.md`,
  `analysis-composite-ship.md`, `roadmap-integration.md` — glob пуст.
- Удалены коммитом `a86247a7` (`T-DOCS01: Аудит и очистка документации —
  ~180 файлов в архив`): все три в stat удаления.
- `CARGO_DIAGNOSIS_2026-06-17.md §TL;DR строка 22` («документация врёт, 3 legacy-дока»)
  — исторический срез на 17.06, сам диагноз при этом актуален (Trade v2 = истина).
- `SHIP_REFACTOR_PLAN P3` строки 168–170 предписывают обновить три файла —
  предписание невыполнимо (файлов нет), сами строки устарели.
  Остальные строки P3 (171–172: legacy-ссылка, Key OVERVIEW статус) — выполнены
  (см. ITERATIONS P2+P3 `3e7aa92`).

## Решение

Статус ПОДТВЕРЖДЕНО ЧАСТИЧНО. Фикс docs-only: баннер в P3-таблице
(«строки 168–170 закрыты как устаревшие: файлы в архиве с T-DOCS01»).
`CARGO_DIAGNOSIS` не трогаем (исторический диагноз, дата в шапке).

## Проверка

- Читать P3-таблицу: баннер на месте.
- Compile: не требуется (docs only).
