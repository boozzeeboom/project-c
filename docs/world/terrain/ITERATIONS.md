# terrain — Iteration Log

## Итерация от 2026-09-20

**Задача (слова пользователя):** в `WorldScene_0_0` нет террейна, геймплей над облаками (1200+), торчат low-poly массивы (`Himalayi_massiv_1_1` и т.п.). Заменить массивы одним полным террейном + добавить схожие/дополнительные массивы. База террейна Y≈600, пики как сейчас — до 5000 (Гималаи).

**Решения пользователя (Q&A):** массивы не удалять, а выключить (`SetActive(false)`); террейн на всю сцену 80 км; без новых FBX («только террейн»); TerrainCollider добавить.

**Коммит:** T-TERR-01 (см. файлы ниже).

**Финальные замеры:** вершины 4987/3492/3003/2825; зазоры: город 300–700, платформа 688, маркет 180 (= оригинал), фермы 240–330, Средняя 326; закопанных объектов 0/7519; PLAYAREA_MAX 2715 (= старый склон); Console 0 errors; скрины: общий план, Примум, фермы.

**Изменения:**
- `Assets/_Project/Scenes/World/WorldScene_0_0.unity` (+111): `Terrain_0_0` под `WorldRoot_0_0`; 4 `Himalayi_massiv_*` → `m_IsActive: false`
- `Assets/_Project/Data/World/Terrain_0_0_TerrainData.asset` (NEW): heightmap 1025, 80000×4400×80000
- `Assets/_Project/Materials/World/Terrain/` (NEW): 2× `.terrainlayer` + 2× `.png` + `.meta`
- `docs/world/terrain/README.md`, `ITERATIONS.md` (NEW)
- НЕ тронут: `BootstrapScene.unity` (в working tree чужое изменение `_dayCycleRealHours 0.001→1` — не наше, не стейджим)

### Ход работ (5 генераций)

1. **v1 — наивные гауссианы по origin инстансов.** Итог: всё село на кламп 5000 (плато на всю карту).
2. **v2 — узкие пики.** Открыл замеры: плато ушло, но фермы/пады зарыты (восточный пик стоял в 1.4 км от истинной вершины).
3. **Замеры FBX:** max-vertex вершины, ray-mesh поверхности под поселениями (город стоит НА склоне main 1991–2346; пады Средней +120; ферма 0_3 +85; фермы 0_1/0_2 висят над голой базой −29).
4. **v3 — пики в истинных вершинах + потолки-«зоны безопасности».** Вершины точные, но потолки дали плоские меза-плато (жалоба пользователя, справедливо).
5. **v4 — снятие потолков.** Ошибка порядка операций: снятие склонов масками + доводка вершин + отроги поверх → двойной учёт (East 4817 вместо 3500), цепь C5 похоронила город (3223 при объектах 2500).
6. **v5 (финал) — сложение без вычитаний:** fitted-склоны + рерут C5 (≥3 км от поселений) + штамп старой поверхности main под play-боксом (91/91 точек, 1:1) + адаптивные отроги + точечный scoop под фермой 0_2 (−300, σ350, хирургически по bbox).

### Ошибки и уроки

- **E1. Пики по origin, не по вершинам.** FBX-«кластеры» асимметричны: вершина East в 1399 м от origin. Правило: только max-vertex world XYZ.
- **E2. Завышенные σ → кламп-плато.** Контролить метрикой `clamped%` (финал 0.00–0.02%).
- **E3. Потолки `min(h,ceiling)`.** Выглядит как выгрызенные плато. Открытость делать fitted-склонами, дистанцией цепей, штампом оригинала.
- **E4. Неверный порядок операций** (снятие → доводка → досыпка = двойной учёт). Порядок: база → герои → цепи → доводка вершин → отроги (только вверх) → clamp.
- **E5. `GetHeight` возвращает мировые единицы** (`size.y`), не нормаль. Чуть не сорвал верификацию.
- **E6. Текстуры в `*Textures*` — мимо git** (`.gitignore:17`). Перенёс `MoveAsset` (GUID сохранены) в `Materials/World/Terrain/`.
- **Удачный приём:** точечный scoop через `GetHeights/SetHeights` по bbox вместо полной регенерации (ферма 0_2: 1923→1627 за секунды).
- **Метрика приёмки:** скан ВСЕХ объектов Y<2600 (7519 шт) на `gap<0`, а не 11 ручных точек — только он нашёл платформу «Основная часть» (644×640 м, была зарыта на 118 м).

## Итерация от 2026-09-20 (T-TERR-02)

**Задача (слова пользователя):** раскидать в самых нижних частях террейна «раскуроченные» примитивы строений — генеративно, без моделинга, дёшево по перфу.

**Коммит:** T-TERR-02 (см. файлы ниже).

**Результат:** 60 хамлетов, 496 инстансов (`WorldRoot_0_0/RuinsValleys`); парящих (>2 м) — 0; Console 0 errors. Тест скриншотами отключён по просьбе пользователя (тестирует сам).

**Изменения:**
- `Assets/_Project/Scenes/World/WorldScene_0_0.unity`: `RuinsValleys` (+496 объектов: MeshFilter+MeshRenderer, shared mesh/material)
- `Assets/_Project/Art/Models/RuinsGen/` (NEW, на диске — `Art/*` вне git, как весь арт): 5 мешей `.asset`
- `Assets/_Project/Materials/World/Ruins/` (NEW): `Ruin_Concrete.mat`, `Ruin_Rust.mat` (URP/Lit, `enableInstancing`), + `.meta`
- `docs/world/terrain/README.md`, `ITERATIONS.md`: раздел про руины

**Перф:** 5 мешей × 2 инстансинг-материала → ~5–7 draw calls; тени выкл, коллайдеров нет, `isStatic=false`.

### Ошибка и фикс (системно, не точечно)

- **E7. Руины тонули/лежали криво.** Три причины в генерации: (1) наклон запекался вокруг не той оси — балка (длинная по Z) наклоном вокруг Z оставалась лежать плашмя; (2) после наклона низ меша уходил ниже pivot'а; (3) высоты брались с грубой 200 м сетки (± метры ошибки). Фикс: LeanX для балки/плиты (балка торчит 5.7 м), `Ground()` (min.y→0) для всех мешей, высота+склон точным семплом террейна в точке (склон >0.35 — отбраковка), заглубление 0.3 + 12% высоты меша. Меши перезаписаны через `EditorUtility.CopySerialized` (тот же сид — детерминировано). Правило: pivot процедурного меша всегда на min.y; сэмплить только точные высоты, грубые сетки — лишь для кандидатов.
